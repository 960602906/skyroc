---
description: State management practices for Skyroc Admin
globs:
  - "src/store/**"
  - "src/features/**"
  - "src/service/hooks/**"
alwaysApply: true
---
# 状态管理规范 State Management

Skyroc Admin 结合 **Redux Toolkit** 与 **TanStack Query** / **ahooks useTable** 构建数据层，遵循以下原则：

## Redux Toolkit
- Store 由 `src/store/index.ts` 通过 `combineSlices` 聚合，slice 放在对应 feature 下。
- 使用 RTK `createSlice` 创建 slice（勿再引入已删除的 `createAppSlice`）：
  ```ts
  export const themeSlice = createSlice({
    name: 'theme',
    initialState,
    reducers: {
      setThemeColor: (state, { payload }: PayloadAction<string>) => {
        state.themeColor = payload;
      }
    },
    selectors: {
      getThemeSettings: state => state
    }
  });
  ```
- 组件内统一使用 `useAppDispatch`、`useAppSelector`（位于 `src/hooks/business/useStore.ts`）。
- **例外：** 非组件模块（axios 错误处理、路由 bootstrap）可直接 `store.dispatch`。
- 当状态需要持久化时，复用 `src/utils/storage.ts` 封装的 `localStg`/`sessionStg`，不要直接访问 `localStorage`。

## 服务端数据：两条路径
- **CRUD 列表/分页**：`features/table` 的 `useTable` / `useTableOperate`（ahooks）。mutation 后用 `run`/`refresh` 刷新，不要求 `invalidateQueries`。
- **非分页缓存**（auth、route、下拉/树选项）：`src/service/hooks` + `QUERY_KEYS`。页面内联 `useQuery` 时也必须用 `QUERY_KEYS`，禁止硬编码 key。

## 何时使用哪一种
- **本地 UI 或需要全局广播的同步状态**：Redux slice。
- **分页 CRUD 列表**：ahooks `useTable`。
- **需缓存的非分页服务端数据**：TanStack Query。

## 其他注意事项
- 所有异步操作需处理异常，使用 `try/catch` 或 Query 的 `onError`，并调用 `window.$message?.error` 展示提示。
- 当新增 slice 时，别忘了在 `src/store/index.ts` 中组合。
