---
description: 档案类新增/编辑/详情页的分区卡片布局（对齐商品档案）
globs:
  - "**/pages/**/operate/**/*.{tsx,ts}"
  - "**/pages/**/detail/**/*.{tsx,ts}"
  - "**/pages/**/list/**/*.{tsx,ts}"
  - "**/pages/**/types/**/*.{tsx,ts}"
alwaysApply: false
---

# 新增 / 编辑 / 详情页布局

档案类全页表单与纯展示详情，统一采用**列表同款分区卡片**：一区一卡、`card-wrapper` + `variant="borderless"`，禁止外层再套一张大卡包住所有分区。

权威样板：

- 新增/编辑：`src/pages/(base)/master/goods/operate/`
- 详情：`src/pages/(base)/master/goods/detail/`
- 列表进详情：`src/pages/(base)/master/goods/list/`、`.../goods/types/`
- 客户（含关签回列表）：`src/pages/(base)/master/customer/operate/`、`.../customer/detail/`

## 页面外壳

```tsx
<div className="h-full min-h-500px flex-col-stretch gap-16px overflow-auto">
  <ACard className="card-wrapper" title={...} extra={操作区} variant="borderless" />
  {/* 下方为分区卡片，彼此 gap-16px */}
</div>
```

| 页面 | 顶栏卡 | 下方分区 |
| --- | --- | --- |
| 新增/编辑 | 标题 + 取消/确认；无正文时可用 `styles={{ body: { display: 'none' } }}` | 表单内多张分区卡 |
| 详情 | 主标题（如名称）+ 返回列表/编辑；正文可放编码等摘要 | `ADescriptions` 分区卡 |

## 分区卡片（必守）

- 按业务拆分，常见三块：`基础信息` / `计量与供应`（或关联信息）/ `销售与状态`（或状态备注）
- 每块独立 `ACard`：`className="card-wrapper"`、`variant="borderless"`、`title` 用 i18n（如 `sectionBasic`）
- 分区之间用父级 `flex-col-stretch gap-16px`，不要用嵌套小卡或 Tab 充当主分区
- 大屏两列（`ACol lg={12}`），描述/备注等通栏；分区内最后一项可加 `className="mb-0"`

## 新增 / 编辑

- 单 `AForm` 包住所有分区卡，一次 `validateFields`
- 树形分类用 `ATreeSelect`；主数据 ID 用 `ASelect` + `service/hooks` 选项，禁止手输 GUID
- 有关联约束时做软联动（例：默认供应商变更时自动并入可供货列表）
- 编辑/详情首屏数据走路由 `loader` + `useLoaderData`（见 route-loader-data 技能），勿 `useEffect` 再拉详情

## 详情（纯展示）

- 路由建议：`.../detail/[id]`；列表点名称进详情，详情再进编辑
- 分区字段与编辑页对齐；空值显示 `-`；状态用现有 badge/tag 渲染
- ID 解析为名称后再展示（分类树 / 单位 / 仓库 / 供应商等）
- **时间字段：** 审计时间戳用 `displayDateTime`（UTC→本地）；业务日历日用 `displayDate`。详见 `.cursor/rules/datetime-display.mdc`

## 离开页：关闭当前页签（必守）

档案类**全页**新增 / 编辑 / 详情离开时，必须**关闭当前页签**再回到列表，禁止只 `nav` 导致页签堆积。

适用时机：

| 操作 | 行为 |
| --- | --- |
| 取消 | 关当前签 → 列表 |
| 返回列表 | 关当前签 → 列表 |
| 新增 / 编辑保存成功 | 关当前签 → 列表 |

```tsx
import { useCloseTabAndNavigate } from '@/features/tab';

const closeTabAndNavigate = useCloseTabAndNavigate();
const listPath = '/master/xxx/list'; // 或该模块列表路径

// 取消 / 返回
<AButton onClick={() => closeTabAndNavigate(listPath)}>{t('common.cancel')}</AButton>

// 保存成功
await fetchAddXxx(values);
window.$message?.success(t('common.addSuccess'));
closeTabAndNavigate(listPath);
```

| 要求 | 说明 |
| --- | --- |
| API | 统一用 `useCloseTabAndNavigate`（`@/features/tab`）：只移除当前签再 `nav(path)`，**不要**走 `removeTabById`（会先跳邻签再跳目标，易闪错误页） |
| 禁止 | 仅 `nav(listPath)` 而不关签；在页面内复制一套关签逻辑；先 `removeTabById` 再 `nav` |
| 范围 | 仅全页 operate/detail；列表页上的 Drawer/Modal 编辑不关签 |
| 详情点「编辑」 | 进入编辑页，**不**关详情签（用户可能返回对照） |

## 列表 → 详情链接（必守）

列表中可点击进入详情的字段（通常为名称），必须使用主题色，禁止写死蓝色或其它色值。

```tsx
// ✅ 推荐：AButton type="link"；颜色随 ConfigProvider 主题（colorLink = colorPrimary）
<AButton
  className="p-0"
  type="link"
  onClick={() => nav(`/master/xxx/detail/${record.id}`)}
>
  {name}
</AButton>
```

| 要求 | 说明 |
| --- | --- |
| 组件 | `AButton type="link"` + `className="p-0"`（表格内去按钮内边距） |
| 颜色 | 跟随项目主题色；全局已在 `features/theme/shared.ts` 将 `colorLink` 对齐 `colorPrimary` |
| 禁止 | 硬编码 `#1677ff` / `color: blue`、用普通文本 + 自定义非主题色、用 `Typography.Link` 却另写色值 |

切换主题色后，列表进详情链接应同步变色，无需改业务页。

## 反例

```tsx
// ❌ 外层大卡再嵌 size="small" 分区卡
<ACard className="card-wrapper" title="编辑">
  <ACard size="small" title="基础信息">...</ACard>
</ACard>

// ❌ 用 Tab 当主信息架构（字段不多时）
<ATabs items={[...]} />

// ❌ 列表进详情链接写死非主题色
<AButton type="link" style={{ color: '#1677ff' }} onClick={...}>
  {name}
</AButton>

// ❌ 取消/返回/保存成功只跳转不关签
<AButton onClick={() => nav('/master/xxx/list')}>{t('common.cancel')}</AButton>
await fetchAddXxx(values);
nav('/master/xxx/list');
```
