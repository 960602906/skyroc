# SkyRoc 前端开发进度

> 唯一任务清单：[接口对接与页面完善清单](./接口对接与页面完善清单.md)。每次开始前先读取本文件，再领取清单中唯一的第一项未完成任务。
>
> 最后核对：2026-07-22。

## 当前断点

**当前唯一任务：M08「采购入库」**

M07 已完成（采购单列表、详情、手工新增/编辑、删除、完成/取消；由计划生成功能待后续对接）。X01 下拉选项分级治理运行时验收已通过，继续菜单任务序列。

## 菜单页面基线

| 状态 | 数量 | 说明 |
| --- | ---: | --- |
| 已完成 | 23 | 已有非占位页面：两项首页看板、订单/售后/取货任务、采购计划与采购单、全部基础资料页面、用户/角色/菜单管理。 |
| 待验收 | 0 | 无待验收项目。 |
| 待页面 | 36 | 页面目录已存在但仍为 `PagePlaceholder`，服务层已有或需在页面任务内收紧。 |
| 待接口 | 2 | 部门管理缺少前端部门树契约；文件管理缺少后端列表/详情/删除契约。两页目前也为占位页。 |
| 待路由 | 2 | 操作日志和登录日志已有后端/API 层，但还没有页面目录。 |
| 阻塞 | 0 | 当前无阻塞项。 |
| 合计 | 63 | 与 `前端菜单分布.md` 的全部叶子菜单一一对应。 |

页面文件实况基线：`PagePlaceholder` 为 38 个；其中部门管理和文件管理因优先接口缺口计为「待接口」，其余 36 个计为「待页面」；操作日志与登录日志没有页面文件。

## 最近完成项

| 任务 | 完成日期 | 结果 |
| --- | --- | --- |
| M01 经营概览 | 2026-07-20 前既有 | 已接入概览、销售趋势、客户排行和商品分类排行；作为已完成页面基线保留。 |
| M02 对账取货 | 2026-07-20 | 已收紧 Dashboard API 类型，接入统计周期、对账汇总和取货状态查询；管理员会话手测筛选、重置与刷新通过。 |
| M03 销售订单 | 2026-07-20 前既有 | 列表、详情、编辑及审核流程已接入。 |
| M04 售后单 | 2026-07-20 前既有 | 售后草稿、审核状态机和操作页已接入。 |
| M05 取货任务 | 2026-07-20 | 取货查询、详情、全页安排司机、开始和完成操作已接入；新增入口复用售后建单，审核后自动生成任务。 |
| X01 下拉选项分级治理（代码完成，运行时验收通过） | 2026-07-22 | 新增通用远程选择器和 ID 回填解析；客户、商品、供应商、报价、协议、销售订单改为默认 20 条防抖搜索，有界资料改为轻量接口，采购计划单位按商品联动；采购计划页 Network 已验证 `bounded`、`search?limit=20`、防抖关键词和 `resolve?ids=`，并通过全量读取静态守卫。 |
| M06 采购计划 | 2026-07-22 | 列表、详情、手工新增、分配供应商/采购员、合并与拆分操作已接入；商品/供应商远程搜索、采购员有界读取、单位随商品联动。运行时 API 验证通过（列表 60 条、筛选、详情、分配供应商），静态核对通过（类型、占位页 39、M 行 63）。 |
| M07 采购单 | 2026-07-22 | types/urls/api 已按后端 DTO 收紧类型；列表、详情、手工新增/编辑（抽屉）、删除、完成/取消已实现；商品远程搜索、单位随商品联动、采购员有界读取；搜索栏与列表布局对齐采购计划风格，新增/编辑改为 `ADrawer` 抽屉（`useTableOperate` + `OperateDrawerProps`，与商品分类一致）；构建通过（build 34.54s、chunk 8.40 kB）、静态核对通过（M 行 63、占位页 38）。由计划生成功能待后续专门对接或单独页面实现。 |
| M06 采购计划（新增改抽屉） | 2026-07-22 | 手工新增从 AModal 改为独立 `PurchasePlanOperateDrawer`（lazy 抽屉），与采购单、商品分类风格一致；分配/合并/拆分/生成等操作仍保留 AModal；构建通过、typecheck 通过。 |

其余已完成基础资料和系统权限页的详细接口与页面位置见任务清单 M40–M55。

## 验证基线

| 日期 | 命令 / 核对 | 结果 |
| --- | --- | --- |
| 2026-07-20 | `pnpm typecheck`（`client/`） | 通过。 |
| 2026-07-20 | `pnpm typecheck`、`pnpm build`（`client/`） | 通过；文件路由已生成取货任务详情和调度页。 |
| 2026-07-20 | 菜单/页面静态盘点 | 63 个叶子菜单、20 个非占位页面、41 个占位页、2 个缺页。 |
| 2026-07-20 | M02：`pnpm typecheck`、改动文件 ESLint、`pnpm dev` 管理员手测 | 已核对 DashboardController 与运行时 OpenAPI；默认统计、周期筛选、重置与刷新均通过。 |
| 2026-07-21 | M06 实况复核：占位页统计、`pnpm typecheck` | M06 已有列表与详情实现，`PagePlaceholder` 实际为 39 个；类型检查通过，运行时验收待 X01 完成后执行。 |
| 2026-07-22 | M06：`pnpm typecheck`、静态核对、admin/123456 登录、API 验证（列表、筛选、详情、分配供应商） | 通过；后端 .NET 9 本地运行；列表 60 条 total，按 `purchaseStatus=1` 筛选 10 条，详情含商品明细，分配供应商返回 code=200。 |
| 2026-07-22 | M07：`pnpm build`、静态核对（M 行 63、占位页 38） | 通过；构建耗时 38.49s，生成采购单页面 chunk 10.85 kB；类型收紧完成，业务逻辑与操作按钮符合规范。i18n 类型因自动生成机制暂未更新，不影响运行。 |
| 2026-07-22 | M07 搜索栏与列表对齐：`pnpm build`、静态核对 | 通过；参照采购计划重构搜索栏（`Page.SearchProps`、`SearchActionsCol`）与列表布局（`CrudPageLayout` + `extra`、`size="small"`）；构建耗时 46.68s、chunk 优化至 10.51 kB。 |
| 2026-07-22 | M06/M07 新增改抽屉：`pnpm typecheck`、`pnpm build`、静态核对 | 通过；采购单新增/编辑、采购计划手工新增均改为 `ADrawer`（与商品分类一致），明细行逻辑下沉到 lazy 抽屉；typecheck 0 错误，build 34.54s（采购单 8.40 kB、采购计划 9.85 kB），M 行 63、占位页 38。 |
| 2026-07-22 | X01/P5-01 Network 收口：`pnpm check:options`、`pnpm typecheck`、`pnpm build`、9529/5293 管理员会话 | 通过；采购计划页实际请求为采购员 `/options/bounded`、供应商 `/options/search?limit=20`、关键词防抖搜索和 `/options/resolve?ids=<guid>`，选择项正确显示中文名称与编码；未出现供应商旧全量列表或固定大页选项请求，生产构建仅保留既有 chunk size 警告。 |

## 每项完成后的更新要求

1. 在任务清单中把当前行改为「已完成」，补充实际新增的页面、接口或测试证据；不能只改变进度数字。
2. 更新本文件的当前唯一任务、各状态统计、最近完成项和验证基线。
3. 运行清单第 3 节的静态核对、`pnpm typecheck`、`git diff --check`；页面改动再以 `pnpm dev` 手动走通本任务主流程。
4. 如后端缺少完成页面所必需的契约，将任务改为「阻塞」，记录 Controller/接口证据和所需决定；不得跳过到下一项。

## 续接口令

以下自然语言均表示从 M02 开始继续本序列：

- “继续前端对接”
- “完善界面”
- “继续完成菜单页面”

执行者无需要求用户再次指定文件名、页面路径或接口名称；应先遵循 `client/AGENT.md` 的续接规则，再以本文件的当前唯一任务为边界实施。
