

```markdown
请严格按以下流程执行：

1. 读取 `docs/接口对接与页面完善清单.md` 和 `docs/前端开发进度.md`。
2. 只领取第一个不是「已完成」也不是「阻塞」的任务（横向任务 X01 不算菜单任务）。
3. 按顺序完整完成该页：types → urls → api → hooks/调用页 → 路由/i18n/权限 → 验收。
   - 仅补 API 函数或仅替换占位页都不能完成任务。
   - 对直接映射的后台 DTO 先收紧 `Api.*` 类型，再调用 `src/service/request`。
   - 业务代码不得读取 `response.data`；列表用 `useTable`，全页详情/编辑首屏使用路由 `loader`。
   - 按钮权限由接口权限与动态路由返回的按钮权限共同控制。
   - 后端业务失败 HTTP 状态仍为 200，前端只按响应体 `code` 判断。
4. 日期/时间展示：审计时间（`createTime`/`updateTime`/`auditTime` 等）用 `displayDateTime`（UTC→本地）；业务日历日（`orderDate`/`receiveDate` 等）用 `displayDate`（不做时区换算）。
5. 新增/编辑/详情用分区卡片，取消 / 返回列表 / 保存成功须 `useCloseTabAndNavigate` 关当前签再回列表。
6. 下拉选项遵循横向任务 X01：增长型数据用限量远程搜索，有边界数据用轻量有界读取，禁止全量实体接口或固定大页模拟选项。
7. 完成后执行静态核对（仓库根目录）：
   - `pnpm typecheck` 通过；
   - 清单中 `| M\d{2} |` 行数应为 63；
   - `src/pages/(base)` 下 `PagePlaceholder` 占位页应为 36 个；
   - `src/pages/(base)/system/ops/operation-logs/index.tsx` 与 `login-logs/index.tsx` 仍不存在。
8. 涉及页面交互时，启动 `pnpm dev`，以有权限的测试账号走通本任务的新增/查询/状态动作或只读筛选主流程。
9. 验收通过后：
   - 将本清单对应行状态改为「已完成」（或「待验收」若无运行时验证条件）；
   - 更新 `docs/前端开发进度.md` 的统计、断点与测试基线；
   - 把下一条未完成项设为唯一当前任务。
10. 全程使用中文回复，并在最后简要说明：本次完成项、下一项 ID、是否阻塞。

请开始。
```

## 使用说明

- 每次发出后 Claude 只会推进一个任务，完成后会等待下一次提示。
- 如遇后端契约缺失或不明，应按清单规则标记「阻塞」并记录证据，不得继续实现。
- 横向任务 X01 不在此提示词范围内，按清单单独管理。
