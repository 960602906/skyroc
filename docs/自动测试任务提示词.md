在 SkyRoc 仓库执行一次聚焦自动业务测试。开始前读取 AGENTS.md、docs/开发进度.md、docs/自动测试任务清单.md 和自动化 memory.md，检查 git
status 并保留已有改动。若检测到本仓库已有 dotnet
test、testhost 或 vstest 进程，立即跳过本轮并汇报，不启动或终止任何测试进程。只执行清单首个未完成任务的一个业务切片，不跳项、不并行。只使用项目专用 PostgreSQL；按稳定业务键幂等补齐长期联调数据，严禁删库、重建、全表 TRUNCATE、清空数据或修改非受管数据。临时数据使用批次标识并精确清理。验证仅运行本次切片对应的精确 FullyQualifiedName 测试、必要的 PostgreSQL 元数据或质量报告测试、dotnet
build 和 dotnet
format；禁止使用 DemoData|DataQuality 等宽泛过滤器，禁止自动运行完整 SkyRoc.Tests 测试项目。约 50 分钟的完整测试只能在用户当前消息明确要求手动触发时执行，不得因任务收口或共享逻辑变更自行启动。失败时保留证据、更新进度且不勾选任务。不要提交、推送或创建 PR。汇报数据增量、质量结果、临时数据清理、实际测试耗时和下一项任务。
