# SkyRoc 自动开发任务清单

> 执行顺序：从上到下，每次自动化只完成第一个未勾选任务。  
> 完成标准：交付物全部落地，相关测试与 `dotnet build SkyRoc.sln`、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj` 通过，文档已同步。仅创建空壳、只写计划或测试失败均不得勾选。

## 执行规则

1. 开始前读取 `AGENTS.md`、`docs/开发进度.md`、本清单及任务对应的 `docs/business-flows/*.md`。
2. 检查 `git status`，保留用户已有改动；领取第一个 `[ ]` 任务，不跳项、不并行领取下一项。
3. 完成实现、迁移、测试和必要文档。任务范围过大时仍只围绕该任务工作，不顺手推进下一项。
4. 验收通过后改为 `[x]`，更新 `docs/开发进度.md` 的断点、日期和测试基线；下一项加粗成为唯一当前任务。
5. 如被外部依赖或关键决策阻塞，不勾选、不跳过；把证据和所需决定写入 `docs/开发进度.md` 后结束本次运行。
6. 注释属于交付物：新增或修改持久化模型时，必须用 EF Core `HasComment` 为数据库表和字段配置可落库注释并检查迁移；新增或修改仓储层、服务层、控制层时，必须为公共类型和成员补齐说明业务语义的 XML 注释。缺少注释不得勾选任务。

## P1 系统权限与基础资料收口

- [x] P1-05 个人中心与密码：提供当前用户资料查询/更新和修改密码 API；只能操作本人，校验旧密码，并补齐服务测试与 HTTP 示例。
- [x] P1-06 权限基础设施：定义稳定的接口权限编码、授权要求和处理器；至少覆盖允许/拒绝两类自动化测试。
- [x] P1-07 系统接口授权：为用户、角色、菜单、按钮、部门接口配置细粒度权限并补齐授权回归测试。
- [x] P1-08 基础资料授权：为商品、客户、定价、采购基础资料接口配置细粒度权限并补齐授权回归测试。
- [x] P1-09 商品定价测试：覆盖商品分类、商品单位、上下架、报价审核、报价商品和协议价客户/商品关系。
- [x] P1-10 客户采购基础测试：覆盖公司、客户、标签、子账号、供应商、采购员、仓库和采购规则关联。
- [x] P1-11 第一阶段契约收口：校准 Swagger 与 `SkyRoc.http`，记录第一阶段 API，运行全量测试并更新 README 阶段状态。

## P2 订单主链路

- [x] P2-01 订单数据模型：新增销售订单、订单明细、审核记录实体和 EF 配置，生成可回滚迁移并添加模型测试。
- [x] P2-02 订单应用模型：新增 DTO、查询参数、映射、验证器和仓储接口/实现，覆盖映射与验证测试。
- [x] P2-03 订单 CRUD：实现订单分页、详情、创建、编辑和删除 API；事务保存主表与明细并添加服务测试。
- [x] P2-04 订单审核状态机：实现提交、通过、驳回和重提，保存审核记录，拒绝非法状态流转并测试。
- [x] P2-05 订单联调收口：补齐订单权限、Swagger、HTTP 主流程和 API 集成测试。
- [x] P2-06 采购计划模型：新增采购计划、明细及订单关联实体、EF 配置和迁移，并添加模型测试。
- [x] P2-06A 全链路注释补全：为全部数据库表和字段配置可落库的 EF Core 注释并生成可回滚迁移；为实体及字段、仓储接口/实现、应用服务接口/实现、控制器及操作补齐准确 XML 注释；新增自动化检查防止注释覆盖回退。
- [x] P2-07 采购计划 API：实现查询、手工新增、从已审核订单生成计划及权限控制，并添加测试。
- [x] P2-08 计划合并拆分：实现采购计划合并、拆分、供应商/采购员分配和状态校验，并添加测试。
- [x] P2-09 采购单模型：新增采购单、采购单明细和计划关联实体、EF 配置和迁移，并添加模型测试。
- [x] P2-10 采购单 API：实现采购单 CRUD、完成/取消状态流转、计划生成采购单和权限，并添加测试。
- [x] P2-11 采购链路联调：补齐采购 Swagger、HTTP 示例和“订单 -> 计划 -> 采购单”集成测试。
- [x] P2-12 库存核心模型：新增库存批次、库存流水、入出库主表/明细、盘点主表/明细和迁移，并添加模型测试。
- [x] P2-13 入库流程：实现采购入库、其他入库、销售退货入库及审核/反审核，原子更新批次和流水并测试。
- [x] P2-14 出库流程：实现销售出库、采购退货出库、其他出库及审核/反审核，校验可用库存并测试。
- [x] P2-15 库存盘点：实现盘点创建、差异计算、审核和调整流水，拒绝重复调整并测试。
- [x] P2-16 库存查询收口：实现库存总览、批次和台账查询，补齐权限、Swagger、HTTP 与集成测试。
- [x] P2-17 配送基础模型：新增司机、承运商、路线、客户路线关系和配送异常实体/迁移及 CRUD 测试。
- [x] P2-18 配送任务：从已审核销售出库生成任务，实现分配司机、路线规划和异常登记并测试。
- [x] P2-19 签收回单：实现配送状态机、签收/回单记录并同步订单状态，拒绝非法流转并测试。
- [x] P2-20 第二阶段联调：覆盖“订单 -> 采购 -> 入库 -> 出库 -> 配送 -> 签收”集成测试并更新阶段文档。

## P3 售后与财务

- [x] P3-01 售后模型：新增售后单、售后商品、审核记录和取货任务实体/迁移，并添加模型测试。
- [x] P3-02 售后流程：实现创建、审核、反审核、退款/退货/补货/换货状态机和权限，并添加测试。
- [x] P3-03 售后库存联动：实现取货任务和销售退货入库衔接，保证幂等并添加集成测试。
- [x] P3-04 客户账单模型：新增客户账单及明细，根据签收与售后结果生成/调整应收，并添加测试。
- [x] P3-05 客户结款：新增结款凭证及明细，实现部分/全部结款、优惠、作废和余额校验，并添加测试。
- [x] P3-06 供应商结算：新增待结单据、结算单及明细，联动采购入库/退货，支持作废并添加测试。
- [x] P3-07 第三阶段联调：覆盖售后、客户结算和供应商结算主链路，补齐 Swagger、HTTP 与阶段文档。

## P4 查询与支撑能力

- [x] P4-01 溯源模型：新增检测报告、报告商品、附件、溯源记录和外部报送日志实体/迁移及测试。
- [x] P4-02 溯源流程：实现报告维护、订单商品追溯、二维码详情和外部报送状态查询，并添加测试。
- [x] P4-03 销售与售后报表：实现商品、分类、客户、区域及售后汇总查询与权限测试。
- [x] P4-04 库存与采购报表：实现日库存、库存商品、采购出入库多维汇总查询与权限测试。
- [x] P4-05 首页驾驶舱：实现销售、对账、取货状态和排行聚合接口，明确统计口径并添加测试。
- [x] P4-06 导入导出任务：实现统一任务模型、模板下载、导入校验、导出文件和任务状态查询，并添加测试。
- [x] P4-07 文件上传：实现安全文件上传，校验类型、大小和权限并测试。
- [x] P4-08 打印能力：实现打印模板、模板字段和订单/采购/出入库/结算打印数据接口，并添加测试。
- [x] P4-09 系统支撑：实现运营设置、通知公告、操作日志和登录日志查询，接入关键操作审计并测试。
- [x] P4-10 全项目收口：运行完整回归，核对业务流程文档、Swagger、README 和部署配置，清理剩余断点。
