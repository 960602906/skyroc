# SkyRoc 测试进度与基线

> 本文只记录测试任务、命令结果、质量报告、环境前置条件和已知测试阻塞。业务实现、迁移和开发断点见 [`开发进度.md`](./开发进度.md)。
>
> 最后核对：2026-07-20。

## 当前测试断点

**当前唯一测试任务：T11「溯源、报表与驾驶舱」**

任务来源：[自动测试任务清单](./自动测试任务清单.md)。T10 已勾选；每次自动业务测试只领取 T11 的一个未覆盖业务切片，不跳到 T12–T14。

下一优先级：T11 销售/售后/库存/采购报表与驾驶舱口径切片（按清单与设计文档拆分领取）。

## 最近验证结果

| 日期 | 范围 | 结果 |
| --- | --- | --- |
| 2026-07-20 | T11 切片 `InspectionReportTraceQrPushPostgreSqlTests.InspectionReport_TraceGenerateQrAnonymityPushLogAndPermissionMatrix_OnPostgreSql` | 通过（约 23s；含 build 约 15s）。覆盖采购入库审核→可送检入库→超数量拒绝→可删报告→正式报告快照与就地更新→销售出库→溯源生成/幂等→报告冻结→匿名二维码白名单→报送日志读取→401/403 权限矩阵；临时批次数据已精确清理。顺带修复检测报告更新时 Clear+Add 触发唯一索引冲突的问题（改为按入库明细/附件顺序就地同步）。未勾选 T11。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（21 项） | 通过（约 33s）；最新质量报告 `SKYROC-AUTOTEST-20260720100959-DA160302F-quality.json`：`skyroc`、80/80 非空表、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 34s）。 |
| 2026-07-20 | T10 切片 `SupplierSettlementPartialFullVoidPostgreSqlTests.SupplierSettlement_PartialFullVoidDiscountAndPermissionMatrix_OnPostgreSql` | 通过（约 13s；含 build 约 18s）。覆盖采购入库审核生成应付→部分结款（付款+优惠）→超额拒绝→零金额校验拒绝→尾款结清→余额独立重算→作废尾款回滚→重复作废拒绝→401/403 权限矩阵；临时批次数据已精确清理。勾选 T10。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（21 项） | 通过（约 28s）；最新质量报告 `SKYROC-AUTOTEST-20260720095625-3241D0445-quality.json`：`skyroc`、80/80 非空表、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 35s）。 |
| 2026-07-20 | T10 切片 `SupplierPayableGenerationPostgreSqlTests.SupplierPayable_PurchaseInReturnGenerationIdempotencyAndPermissionMatrix_OnPostgreSql` | 通过（约 13s；含 build 约 29s）。覆盖采购入库草稿无应付→审核生成正向应付→重复审核拒绝→采购退货审核生成负向应付→明细独立重算→反审核移除应付→401/403 权限矩阵；临时批次数据已精确清理。未勾选 T10。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（26 项） | 通过（约 35s）；最新质量报告 `SKYROC-AUTOTEST-20260720095110-390D2C686-quality.json`：`skyroc`、80/80 非空表、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 40s）。 |
| 2026-07-20 | T10 切片 `AfterSaleReceivableOffsetPostgreSqlTests.AfterSale_ReceivableOffsetIdempotencyAndPermissionMatrix_OnPostgreSql` | 通过（约 28s；含 build 约 17s）。覆盖其他入库→销售订单审核→销售出库→配送签收生成客户账单→仅退款草稿提交/审核→完成前账单无冲减→完成后负向明细与应收独立重算→重复完成拒绝且不重复冲减→401/403 权限矩阵；临时批次数据已精确清理。未勾选 T10。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（26 项） | 通过（约 33s）；最新质量报告 `SKYROC-AUTOTEST-20260720094316-252660A4B-quality.json`：`skyroc`、80/80 非空表、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，`demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 36s）。 |
| 2026-07-20 | T10 切片 `CustomerSettlementPartialFullVoidPostgreSqlTests.CustomerSettlement_PartialFullVoidDiscountAndPermissionMatrix_OnPostgreSql` | 通过（约 21s；含 rebuild 约 8s）。覆盖其他入库→销售订单审核→销售出库→配送签收生成客户账单→部分结款（收款+优惠）→超额拒绝→零金额校验拒绝→尾款结清→余额独立重算→作废尾款回滚→重复作废拒绝→401/403 权限矩阵；临时批次数据已精确清理。未勾选 T10。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（26 项） | 通过（约 36s）；最新质量报告 `SKYROC-AUTOTEST-20260720093353-58E411FE8-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 38s）。 |
| 2026-07-20 | T9 切片 `SalesReturnInboundReplenishPostgreSqlTests.SalesReturn_InboundReplenishIdempotencyAndPermissionMatrix_OnPostgreSql` | 通过（约 23s；含 build 约 17s）。覆盖其他入库→销售订单审核→退货退款审核生成取货→未完成取货拒绝入库→分配/开始/完成取货→未入库拒绝完成售后→销售退货入库草稿幂等与分歧拒绝→审核回补批次/台账→重复审核拒绝→完成售后→完成后反审核拒绝→401/403 权限矩阵；临时批次数据已精确清理。勾选 T9。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（26 项） | 通过（约 32s）；最新质量报告 `SKYROC-AUTOTEST-20260720092628-31097CDC6-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 34s）。 |
| 2026-07-20 | T9 切片 `PickupFulfillmentPostgreSqlTests.Pickup_AssignStartCompletePermissionAndIllegalTransitions_OnPostgreSql` | 通过（约 18s；含 rebuild 约 8s）。覆盖其他入库→销售订单审核→退货退款审核生成取货→未分配拒绝开始/完成→禁用司机拒绝→分配/换司机→开始→取货中拒绝再分配→完成→完成后拒绝再流转→401/403 权限矩阵；售后仍为待退货且未衔接入库；临时批次数据已精确清理。未勾选 T9。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（21 项） | 通过（约 28s）；质量报告 `SKYROC-AUTOTEST-20260720092013-4D7A8B3A2-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 34s）。 |
| 2026-07-20 | T9 切片 `AfterSaleTypeStateMachinePostgreSqlTests.AfterSale_TypeStateMachinePermissionAndReservation_OnPostgreSql` | 通过（约 42s；含 build 约 17s）。覆盖其他入库→销售订单审核→仅退款草稿提交/驳回/重提/审核/反审/完成→退货退款审核生成取货且反审拒绝→补货/换货待退货→累计数量超限与未审核订单拒绝→401/403 权限矩阵；临时批次数据已精确清理。未勾选 T9。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（21 项） | 通过（约 31s）；质量报告 `SKYROC-AUTOTEST-20260720091411-65A7BF021-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 34s）。 |
| 2026-07-20 | T8 切片 `DeliverySignAndReturnReceiptAggregationPostgreSqlTests.Delivery_SignAndReturnReceiptAggregatesOrderBillAndReturnStatus_OnPostgreSql` | 通过（约 26s；含 build 约 35s）。覆盖其他入库→销售订单→两张分批销售出库→未开始签收/未签收回单拒绝→首任务签收后订单仍 Delivering 且无账单→末任务签收后订单 Signed、结算金额与客户账单生成→分次回单聚合为 Returned→重复签收/回单与超额验收拒绝→401/403 签收/回单权限矩阵；临时批次数据已精确清理。勾选 T8。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（21 项） | 通过（约 35s）；最新质量报告 `SKYROC-AUTOTEST-20260720090715-B187D02BF-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 34s）。 |
| 2026-07-20 | T8 切片 `DeliveryExceptionAndPartialDeliveryPostgreSqlTests.Delivery_ExceptionLifecycleAndPartialSignKeepsOrderOpen_OnPostgreSql` | 通过（约 26s；含 build 约 44s）。覆盖其他入库→销售订单→两张分批销售出库→待分配拒绝异常→分配/开始后双异常登记与分次处理恢复 Delivering→重复处理拒绝→首任务签收后订单仍 Delivering 且无客户账单→401/403 异常创建/处理权限矩阵；临时批次数据已精确清理。未勾选 T8。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（21 项） | 通过（约 35s）；质量报告 `SKYROC-AUTOTEST-20260720085904-C5843A35E-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.Tests`（临时输出目录） | 通过，0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 35s）。 |
| 2026-07-20 | T8 切片 `DeliveryTaskAssignRoutePostgreSqlTests.DeliveryTask_AssignDriverIntelligentPlanStartAndPermissionMatrix_OnPostgreSql` | 通过（约 20s；含 build 约 26s）。覆盖其他入库→销售出库→生成任务→禁用司机拒绝→分配司机快照→智能规划路线→换司机覆盖→开始配送并同步订单 Delivering→配送中拒绝再分配/规划→401/403 更新权限矩阵；临时批次数据已精确清理。未勾选 T8。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（14 项） | 通过（约 23s）；最新质量报告 `SKYROC-AUTOTEST-20260720085152-6528BAE28-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.sln`（临时输出目录） | 通过，0 错误（解决方案级 `-o` 有 NETSDK1194 警告，可忽略）。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 41s）。 |
| 2026-07-20 | T8 切片 `DeliveryTaskGenerateFromStockOutPostgreSqlTests.DeliveryTask_GenerateFromSaleStockOutSnapshotIdempotencyAndPermissionMatrix_OnPostgreSql` | 通过（约 17s；含 rebuild 约 22s）。覆盖其他入库建批次→销售订单审核→销售出库审核→生成配送任务（PendingAssign + 快照）→幂等复用→草稿/其他出库拒绝→401/403 权限矩阵；临时批次数据已精确清理。未勾选 T8。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（14 项） | 通过（约 27s）；质量报告 `SKYROC-AUTOTEST-20260720084433-5442B8220-quality.json`：`skyroc`、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.sln`（临时输出目录） | 通过，0 错误（解决方案级 `-o` 有 NETSDK1194 警告，可忽略）。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 32s）。 |
| 2026-07-20 | T7 收口 `StockConsistencyClosurePostgreSqlTests.Stock_CostBatchLedgerLockStocktakingConsistency_OnPostgreSql` | 通过（约 13s；含 build 约 26s）。全库批次非负、可用≤账面、流水净额/最新余额对齐、盘点差异与台账对应、`stockBatchQuantitiesAreNonNegative` 与 `demoDataAcceptance.isReady=true`。本切片无临时写入；全量 `GenerateDemoDataAsync` 因受管菜单按钮 `SKYROC-DEMO-MENU-BUTTON-001` 字段漂移失败，未改库，收口改为只读核对既有联调库存。勾选 T7。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（11 项） | 通过（约 24s + 0.7s）；最新质量报告 `SKYROC-AUTOTEST-20260720083216-CCAA51EEA-quality.json`：`skyroc`、80 张非空表、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，库存非负与 `isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.sln`（临时输出目录） | 通过，0 错误（解决方案级 `-o` 有 NETSDK1194 警告，可忽略）。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 34s）。 |
| 2026-07-20 | 测试库白名单 `neondb` → `skyroc`；连接优先 `appsettings.Development.json` | 配置与守卫单测通过；质量报告 `databaseName=skyroc`。 |
| 2026-07-20 | `StockConcurrencyIdempotencyRollbackPostgreSqlTests.Stock_ConcurrencyIdempotencyAndRollback_OnPostgreSql` | 通过（约 12s；含 build 约 31s）。覆盖入库双审核、超额出库并发扣减、出库双反审核、幂等拒绝、失败更新与精确清理。顺带修复 `StockBatchRepository.GetByIdentityForUpdateAsync` 在已跟踪批次上吞掉 FOR UPDATE 新值的并发双胜问题。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN + 白名单守卫（9 项） | 通过（约 22s）；最新质量报告 `SKYROC-AUTOTEST-20260720082427-E40FBD5E4-quality.json`：`skyroc`、80/80 非空表、临时残留/元数据/孤儿外键/重复编码 findings 均为 0，`demoDataAcceptance.isReady=true`。 |
| 2026-07-20 | `dotnet build SkyRoc.sln`（临时输出目录） | 通过，0 错误（解决方案级 `-o` 有 NETSDK1194 警告，可忽略）。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过（约 35s）。 |
| 2026-07-20 | `PurchaseReturnAndSalesReturnFlowPostgreSqlTests.PurchaseReturn_SalesReturn_FlowBatchSourceConservationAndPermissionMatrix_OnPostgreSql` | 通过；覆盖采购退货出库和销售退货入库审核/反审核、来源守恒、权限矩阵及清理。 |
| 2026-07-20 | `pnpm typecheck`（`client/`） | 通过；前端菜单对接文档基线为 63 个页面任务。 |

## 回归基线与环境说明

| 项目 | 当前记录 |
| --- | --- |
| 最近完整 .NET 测试基线 | 453 通过、0 跳过、约 50 分 3 秒；仅在用户明确要求时才运行完整 `SkyRoc.Tests` 项目。 |
| 当前聚焦策略 | 只运行当前业务切片的精确 FullyQualifiedName、必要的 PostgreSQL 质量/元数据测试、`dotnet build` 和 `dotnet format`；禁止用宽泛 filter 替代切片验证。 |
| 本地完整测试异常 | 本轮再次执行为 442 通过、73 失败；失败由现有环境门禁导致，不应直接判为本轮回归：3 项源码/部署配置门禁、未设置 `SKYROC_QUERY_TEST_CONNECTION_STRING` 的 2 项只读 PostgreSQL 查询测试。默认输出目录还被运行中的 `SkyRoc.exe` 锁定；已通过仓库内 `artifacts/test-bin-*` 输出目录完成本轮定向构建和测试。 |
| 专用测试库规则 | 只使用受控 PostgreSQL 测试库 `skyroc`（与 `appsettings.Development.json` 一致，白名单见 `postgresql-testsettings.json`）；严禁删库、重建、全表 `TRUNCATE` 或修改非受管长期数据。跨请求测试必须使用本轮批次号并按外键逆序精确清理。 |
| 最新质量基线 | 质量报告 `SKYROC-AUTOTEST-20260720100959-DA160302F-quality.json`：`demoDataAcceptance.isReady=true`，孤儿外键、重复业务编码、临时残留、元数据问题和库存非负检查均通过。 |

## 测试记录规范

每次测试只在本文追加一条简洁记录，包含：

1. 日期、任务/业务切片和精确测试类或 FQN。
2. 实际执行命令、通过/失败数量和耗时。
3. 数据质量报告名称、临时数据清理结果和重要副作用断言。
4. 环境阻塞或已知失败的证据；失败任务不得勾选。
5. 下一项唯一测试任务或当前任务的下一切片。

不要在本文重复业务实现过程、Controller/DTO 设计或前端页面信息；这些内容更新到 [`开发进度.md`](./开发进度.md) 或 `client/docs/` 的前端进度文档。

## 测试续接规则

1. 开始前读取本文件、[`自动测试任务清单.md`](./自动测试任务清单.md)、`AGENTS.md` 和相关业务流程文档；检查 `git status`。
2. 若已有 `dotnet test`、testhost 或 vstest 进程，本轮跳过，不终止现有进程。
3. 严格遵守自动测试任务清单的安全边界、数据准备、精确清理和命令范围。
4. 业务测试完成后先更新本文的结果/基线；若同时改变了实现状态，再单独更新 [`开发进度.md`](./开发进度.md)。
