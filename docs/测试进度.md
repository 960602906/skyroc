# SkyRoc 测试进度与基线

> 本文只记录测试任务、命令结果、质量报告、环境前置条件和已知测试阻塞。业务实现、迁移和开发断点见 [`开发进度.md`](./开发进度.md)。
>
> 最后核对：2026-07-20。

## 当前测试断点

**当前唯一测试任务：T7「入库、出库、库存和盘点」**

任务来源：[自动测试任务清单](./自动测试任务清单.md)。每次自动业务测试只领取 T7 的一个未覆盖业务切片，不跳到 T8–T14。

已覆盖的 T7 切片：

- 其他入库审核、批次创建、移动加权成本和反审核回滚。
- 其他出库审核锁定/扣减、库存不足拒绝和反审核恢复。
- 盘点盘盈盘亏、快照失效拒绝、调整台账和权限矩阵。
- 采购退货出库与销售退货入库的来源守恒、批次和成本回滚。

下一优先级：补齐并发/幂等边界（重复审核、并发库存扣减、并发反审核或相同来源重复处理）；只有任务清单的成本、批次、流水、锁定、盘点一致性门槛全部满足后才能勾选 T7。

## 最近验证结果

| 日期 | 范围 | 结果 |
| --- | --- | --- |
| 2026-07-20 | `PurchaseReturnAndSalesReturnFlowPostgreSqlTests.PurchaseReturn_SalesReturn_FlowBatchSourceConservationAndPermissionMatrix_OnPostgreSql` | 通过；覆盖采购退货出库和销售退货入库审核/反审核、来源守恒、权限矩阵及清理。 |
| 2026-07-20 | PostgreSQL 质量/元数据精确 FQN（2 项） | 通过；质量报告 `SKYROC-AUTOTEST-20260720031838-77308AB32-quality.json` 显示 80 张业务表、80 张非空表、临时残留与一致性 findings 均为 0。 |
| 2026-07-20 | `dotnet build SkyRoc.sln --no-restore` | 通过，0 警告、0 错误。 |
| 2026-07-20 | `dotnet format SkyRoc.sln --verify-no-changes` | 通过。 |
| 2026-07-20 | `pnpm typecheck`（`client/`） | 通过；前端菜单对接文档基线为 63 个页面任务。 |

## 回归基线与环境说明

| 项目 | 当前记录 |
| --- | --- |
| 最近完整 .NET 测试基线 | 453 通过、0 跳过、约 50 分 3 秒；仅在用户明确要求时才运行完整 `SkyRoc.Tests` 项目。 |
| 当前聚焦策略 | 只运行当前业务切片的精确 FullyQualifiedName、必要的 PostgreSQL 质量/元数据测试、`dotnet build` 和 `dotnet format`；禁止用宽泛 filter 替代切片验证。 |
| 本地完整测试异常 | 曾记录 442 通过、73 失败；失败由现有环境门禁导致，不应直接判为本轮回归：3 项源码/部署配置门禁、未设置 `SKYROC_QUERY_TEST_CONNECTION_STRING` 的 2 项只读 PostgreSQL 查询测试，以及当前数据库名不在测试精确白名单导致的其余 PostgreSQL 集成测试。 |
| 专用测试库规则 | 只使用受控 PostgreSQL 测试库；严禁删库、重建、全表 `TRUNCATE` 或修改非受管长期数据。跨请求测试必须使用本轮批次号并按外键逆序精确清理。 |
| 最新质量基线 | 质量报告 `SKYROC-AUTOTEST-20260720031838-77308AB32-quality.json`：`demoDataAcceptance.isReady=true`，孤儿外键、重复业务编码、临时残留、元数据问题和库存非负检查均通过。 |

## 测试记录规范

每次测试只在本文追加一条简洁记录，包含：

1. 日期、任务/业务切片和精确测试类或 FQN。
2. 实际执行命令、通过/失败数量和耗时。
3. 数据质量报告名称、临时数据清理结果和重要副作用断言。
4. 环境阻塞或已知失败的证据；失败任务不得勾选。
5. 下一项唯一测试任务或 T7 的下一切片。

不要在本文重复业务实现过程、Controller/DTO 设计或前端页面信息；这些内容更新到 [`开发进度.md`](./开发进度.md) 或 `client/docs/` 的前端进度文档。

## 测试续接规则

1. 开始前读取本文件、[`自动测试任务清单.md`](./自动测试任务清单.md)、`AGENTS.md` 和相关业务流程文档；检查 `git status`。
2. 若已有 `dotnet test`、testhost 或 vstest 进程，本轮跳过，不终止现有进程。
3. 严格遵守自动测试任务清单的安全边界、数据准备、精确清理和命令范围。
4. 业务测试完成后先更新本文的结果/基线；若同时改变了实现状态，再单独更新 [`开发进度.md`](./开发进度.md)。
