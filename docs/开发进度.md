# SkyRoc 业务模块与开发进度

> 最后核对：2026-07-14（T2 已补齐权限、基础资料、定价、配送基础、部门、系统支撑、销售订单、采购计划、采购单、采购入库、销售出库、配送签收、售后取货、销售退货入库、客户账单正向应收与售后冲减、客户结款、供应商结算、检测报告、报告商品/附件、溯源记录、库存批次、库存流水及供应商待结账单联调资料层，整体 T2 尚未验收）
> 核对范围：`docs/business-flows/`、数据库模型与迁移、应用服务、控制器和测试。业务规格中出现过的旧 Vue 接口不等于本仓库已经实现。

## 状态说明

- ✅ 已实现：主要后端路径已经存在，可进入联调或扩展。
- 🟡 部分实现：只有基础资料或部分子流程，尚未形成业务闭环。
- ⬜ 未开始：当前代码中没有对应实体、服务和 API。

## 当前开发断点

**当前阶段：P1 至 P4 计划任务全部完成，当前无未完成自动开发任务。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮只补齐检测报告、报告商品/附件与溯源记录长期联调数据：新增 `DemoDataTraceabilityBuilder`，从 `SKYROC-DEMO-PURCHASE-STOCK-IN-001` 至 `040` 完整稳定备注精确匹配的 40 张受管已审核采购入库出发，经 `ITraceabilityService.CreateInspectionReportAsync` 幂等形成 `SKYROC-DEMO-INSPECTION-REPORT-001` 至 `050` 共 50 张检测报告、100 条报告商品和 100 个附件快照；前 40 张逐单覆盖采购入库，第 41–50 张作为第 21–30 张采购入库的后续复检，报告结论覆盖合格 40、待定 5、不合格 5。为避免伪造批次来源或改写既有已签收链路，生成器另经 `ISaleOrderService` 与 `IStockOutService` 创建 `SKYROC-DEMO-TRACE-SALE-ORDER-001` 至 `020` 及对应 20 张已审核销售出库，逐条扣减报告覆盖的真实采购批次，再调用 `GenerateSaleOrderTracesAsync` 形成 20 条溯源记录；溯源稳定备注只对完整受管订单来源生成的记录受控补齐。复用前完整校验采购来源、商品/单位/批次、数量、结论、附件、销售订单、出库、报告关联和审计指纹，检测到未知稳定键或字段漂移时直接停止；一次安全门禁按预期阻止了既有第 31–40 张报告结论漂移，未覆盖长期数据。相对上一基线长期数据净增 410 条；第二次以及最终巡检的数据库主键、随机业务编号、状态、金额和审计快照完全不变，新增为 0。最新质量报告 `SKYROC-AUTOTEST-20260714124432-B59163D94-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、72 张非空表、4,515 条长期记录，`inspection_report=50`、`inspection_report_goods=100`、`inspection_attachment=100`、`trace_record=20`、`sale_order=80`、`stock_out_order=60`、`stock_ledger=280`；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留、元数据问题和十项一致性门禁失败均为 0。整体仍有 21 项数量/业务链路缺口，故 T2 保持未勾选。检测报告与溯源精确 FQN 初始 RED 按预期失败（期望 40、实际 0，墙钟 209.4 秒），质量门禁修正 RED 按预期失败（期望 50、实际 40，墙钟 199.3 秒），最终 GREEN 1 项通过（测试持续 3 分 38 秒，墙钟 224.1 秒）；精确 PostgreSQL 质量与元数据测试 2 项通过（测试持续 42 秒，墙钟 48.9 秒），`dotnet build SkyRoc.sln --no-restore` 0 警告/错误（墙钟 5.2 秒），`dotnet format SkyRoc.sln --verify-no-changes` 通过（墙钟 33 秒），`git diff --check` 通过；按每日聚焦策略未运行完整测试项目。下一次仍严格领取 T2，优先补齐安全文件元数据及商品图片，再继续导入导出任务与外部报送日志数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮只补齐供应商结算凭证及明细长期联调数据：`DemoDataGenerator` 仅负责协调与计数汇总，`DemoDataSupplierSettlementBuilder` 从 `SKYROC-DEMO-PURCHASE-STOCK-IN-001` 至 `040` 完整稳定备注精确匹配的 40 张受管采购入库供应商待结单据出发，经 `ISupplierSettlementService.CreateAsync` 和 `VoidAsync` 幂等形成 `SKYROC-DEMO-SUPPLIER-SETTLEMENT-001` 至 `100` 共 100 张凭证及 100 条明细；001–040 为每张待结单据首笔四分之一核销，041–060 结清前 20 张单据，061–080 对后 20 张单据继续部分核销，081–100 在后 20 张单据上创建后作废并原子回滚余额，最终覆盖作废 20 张、部分结款 60 张、已结 20 张，40 张来源待结单据保持部分结款 20 张、已结 20 张。生成器要求既有受管流水号构成无缺口前缀，复用前校验完整来源、金额、状态、日期、备注和审计指纹，并独立按所有有效受管明细重算待结单据已结金额与状态；检测到非受管有效结算或稳定键漂移时直接停止，不删除、重建、截断或修改非受管数据。相对上一基线长期数据净增 200 条；第二次生成数据库主键、金额、状态及审计快照完全不变，新增为 0。最新质量报告 `SKYROC-AUTOTEST-20260714111643-AE83B1E07-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、68 张非空表、4,105 条长期记录，`supplier_settlement=100`、`supplier_settlement_detail=100`；供应商待结状态为部分结款 20 张、已结 20 张，供应商结算状态为作废 20 张、部分结款 60 张、已结 20 张，孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留、元数据问题和十项一致性门禁失败均为 0。整体仍有 27 项数量/业务链路缺口，故 T2 保持未勾选。供应商结算精确 FQN RED 按预期失败（期望 100、实际 0，墙钟 222.2 秒），最终 GREEN 1 项通过（测试持续 4 分 49 秒，墙钟 295.748 秒）；精确 PostgreSQL 质量与元数据测试 2 项通过（测试持续 34 秒，墙钟 40.041 秒），`dotnet build SkyRoc.sln --no-restore` 0 警告/错误（墙钟 6.609 秒），`dotnet format SkyRoc.sln --verify-no-changes` 通过（墙钟 34.312 秒），`git diff --check` 通过；按每日聚焦策略未运行完整测试项目。下一次仍严格领取 T2，优先补齐检测报告、报告商品/附件与溯源记录，再继续文件及导入导出业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮续接并收口售后、取货任务、销售退货入库与客户账单冲减单一业务切片：`DemoDataGenerator` 基于 40 张受管已签收销售订单，按 `SKYROC-DEMO-AFTER-SALE-001` 至 `040` 完整稳定备注幂等形成 40 张售后单、40 条售后商品、75 条只追加审核轨迹和 25 张退货取货任务，状态覆盖草稿、待审核、待退款、待退货各 5 张及已完成 20 张；20 张已完成退货退款售后经受管司机分配、开始和完成取货后，通过 `IStockInService.CreateSalesReturnAsync` 与 `AuditAsync` 形成 20 张销售退货入库、20 条明细、20 个退货批次和 20 条入库流水，并在售后完成时向既有客户账单追加 20 条负向调整明细。来源订单由前缀匹配收紧为 60 个完整稳定键精确集合，售后生成计数由 18 项长元组改为命名结果对象，避免误选非受管来源和同类型计数静默错位；生成器未删除、重建、截断或修改无法确认归属的数据。该切片相对配送签收基线长期数据累计净增 280 条；本次自动运行前数据库已具备这些记录，连续幂等巡检后净增为 0。最新质量报告 `SKYROC-AUTOTEST-20260714084751-BFCD9AF6E-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、64 张非空表、3,705 条长期记录，`after_sale=40`、`after_sale_goods=40`、`after_sale_audit_log=75`、`pickup_task=25`、`stock_in_order=100`、`stock_in_detail=180`、`stock_batch=180`、`stock_ledger=260`、`customer_bill_detail=100`；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和一致性门禁失败均为 0，整体仍有 31 项数量/业务链路缺口，故 T2 保持未勾选。售后切片最终精确 FQN 测试 1 项通过（2 分 42 秒），精确 PostgreSQL 质量与元数据测试 2 项通过（43 秒），`dotnet build SkyRoc.sln --no-restore` 0 警告/错误，`dotnet format SkyRoc.sln --verify-no-changes` 通过；按每日聚焦策略未运行完整测试项目。下一次仍严格领取 T2，优先补齐客户结款凭证及明细长期联调数据，再继续供应商结算、溯源、文件与导入导出业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 补齐配送任务、签收回单和客户账单长期联调数据：新增失败用例先确认 `SKYROC-DEMO-DELIVERY-TASK-001` 至 `040` 受管配送任务、40 张签收回单、80 条验收明细、40 张客户账单和 80 条客户账单明细为 0，再由 `DemoDataGenerator` 基于 40 张已审核受管销售出库，经 `IDeliveryTaskService.GenerateFromStockOutAsync` 幂等生成配送任务，调用 `AssignDriverAsync` 绑定受管司机和承运商快照，调用 `IntelligentPlanAsync` 固化客户路线与路线顺序，调用 `StartDeliveryAsync` 推进配送中，调用 `SignAsync` 全量验收本次出库行并同步销售订单签收与客户账单，最后调用 `ReturnReceiptAsync` 归档电子回单并同步订单回单状态。配送任务使用来源销售出库唯一约束和完整稳定备注定位，回单、验收明细和客户账单只随受管任务创建或补齐审计字段，第二次运行配送任务、回单、验收明细、客户账单和账单明细新增均为 0；生成器未删除、重建、截断或修改无法确认归属的数据。为避免重复运行破坏已签收链路，受管销售订单状态校准不再把已配送中或已签收订单回退为待分拣，采购计划和销售出库来源选择同步接受已审核后的下游状态。本轮长期数据净增 280 条。最新质量报告 `SKYROC-AUTOTEST-20260714063422-5AE202C18-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、60 张非空表、3,425 条长期记录，`delivery_task=40`、`order_receipt=40`、`order_check_detail=80`、`customer_bill=40`、`customer_bill_detail=80`；配送任务、回单、验收明细和客户账单适用业务字段填充率均为 100%，客户账单明细仅售后来源字段因当前均为订单验收来源不适用；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和一致性门禁失败均为 0。整体仍有 34 项数量/业务链路缺口，故 T2 保持未勾选。配送签收层 RED 用例先按预期失败（期望 40、实际 0），GREEN 后通过；过宽 `DemoData|DataQuality` 过滤器在 30 分钟窗口超时后已终止确认归属的残留测试进程，改用聚焦配送生成器测试和质量报告测试通过；`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 453 项通过且 0 跳过（耗时 50 分 3 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续售后、取货任务、销售退货入库、客户结款、供应商结算、溯源、文件与导入导出业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 补齐销售出库长期联调数据：新增失败用例先确认 `SKYROC-DEMO-SALE-STOCK-OUT-001` 至 `040` 受管销售出库为 0，再由 `DemoDataGenerator` 基于 40 张已审核受管销售订单，通过 `IStockInService.CreateOtherAsync` 为订单同仓生成 40 张受管销售出库支撑入库并审核，形成 80 条支撑入库明细、80 个稳定销售支撑批次和 80 条支撑入库流水；随后通过 `IStockOutService.CreateSaleAsync` 与 `AuditAsync` 创建并审核 40 张受管销售出库，形成 80 条销售出库明细和 80 条销售出库流水，回写销售订单出库状态。支撑入库和销售出库均使用完整稳定备注定位，第二次运行销售出库、明细和流水新增均为 0；生成器未删除、重建、截断或修改无法确认归属的数据。本轮长期数据净增 480 条。最新质量报告 `SKYROC-AUTOTEST-20260714034151-7AA437186-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、55 张非空表、3,145 条长期记录，`stock_in_order=80`、`stock_in_detail=160`、`stock_batch=160`、`stock_ledger=240`、`stock_out_order=40`、`stock_out_detail=80`、`supplier_bill=40`、`supplier_bill_detail=80`；销售出库主表、明细、支撑入库、库存批次和库存流水适用业务字段填充率均为 100%，孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和一致性门禁失败均为 0。整体仍有 35 项数量/业务链路缺口，故 T2 保持未勾选。销售出库层 RED 用例先按预期失败（期望 40、实际 0），GREEN 后通过；过宽 `DemoData|DataQuality` 过滤器在 30 分钟窗口超时后已终止确认归属的残留测试进程，并改用聚焦验证通过 4 项；`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 452 项通过且 0 跳过（耗时 41 分 26 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续配送任务、签收回单、售后、客户账单、客户结款、供应商结算、溯源、文件与导入导出业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 补齐采购单长期联调数据：新增失败用例先确认 `SKYROC-DEMO-PURCHASE-ORDER-001` 至 `050` 受管采购单为 0，再由 `DemoDataGenerator` 通过 `IPurchaseOrderService.GenerateFromPlansAsync` 基于 40 张受管采购计划生成采购单，并用 `IPurchaseOrderService.UpdateAsync` 补齐采购价格、生产日期、商品快照和明细备注后调用 `CompleteAsync` 形成后续采购入库来源；另通过公开 `CreateAsync` 生成 10 张手工补货采购单，其中 5 张保留草稿、5 张调用 `CancelAsync` 覆盖取消状态。每张采购单使用完整稳定备注定位，含 2 条采购明细；计划来源采购单共 80 条 `purchase_order_plan_rel`，采购计划和明细已回写为已生成及已采购数量。第二次生成采购单、明细和来源关系新增均为 0；生成器只按完整稳定备注定位受管采购单，未删除、重建、截断或修改无法确认归属的数据。本轮长期数据净增 230 条。最新质量报告 `SKYROC-AUTOTEST-20260713110205-0D581E29B-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、47 张非空表、2,265 条长期记录，`purchase_order=50`、`purchase_order_detail=100`、`purchase_order_plan_rel=80`，采购单主表、明细和来源关系适用业务字段填充率均为 100%；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和一致性检查失败均为 0。整体仍有 39 项数量/业务链路缺口，故 T2 保持未勾选。采购单层 RED 用例先按预期失败（期望 50、实际 0），GREEN 后通过；`DemoData|DataQuality` 相关测试 25 项通过（首次 15 分钟窗口超时后以长窗口重跑通过）、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 450 项通过且 0 跳过（耗时 19 分 47 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续采购入库、库存批次、库存流水、销售出库、配送任务、售后、财务与溯源业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 补齐采购计划长期联调数据：新增失败用例先确认 40 条 `SKYROC-DEMO-PURCHASE-PLAN-*` 受管采购计划为 0，再由 `DemoDataGenerator` 基于 40 张已审核受管销售订单逐单调用 `IPurchasePlanService.GenerateFromOrdersAsync` 生成采购计划，并调用公开服务分配供应商和采购员快照；每张计划使用完整稳定业务备注定位，含 2 条计划明细和 2 条来源订单关系，计划明细补齐业务备注、商品/单位快照、需求数量、计划数量和创建审计。第二次生成采购计划、明细和来源关系新增均为 0；生成器只按完整稳定备注定位受管计划，未删除、重建、截断或修改无法确认归属的数据。本轮长期数据净增 200 条。最新质量报告 `SKYROC-AUTOTEST-20260713094816-5CBB54D54-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、44 张非空表、2,035 条长期记录，`purchase_plan=40`、`purchase_plan_detail=80`、`purchase_plan_order_rel=80`；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和一致性检查失败均为 0。整体仍有 41 项数量/业务链路缺口，故 T2 保持未勾选。采购计划层 RED 用例先按预期失败（期望 40、实际 0），GREEN 后通过；`DemoData|DataQuality` 相关测试 24 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 449 项通过且 0 跳过（耗时 15 分 45 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续采购单、库存、配送任务、售后、财务与溯源业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 补齐销售订单长期联调数据：新增失败用例先确认 `SKYROC-DEMO-SALE-ORDER-001` 至 `060` 受管订单为 0，再由 `DemoDataGenerator` 在基础资料事务提交后经 `ISaleOrderService` 创建 60 张受管销售订单和 120 条订单明细，订单内部备注保存完整稳定键用于幂等定位；订单状态覆盖待审核 10 张、待分拣 40 张、已驳回 10 张，并由服务写入提交、通过或驳回审核轨迹，当前 `order_audit_log=110`。第二次生成销售订单与明细新增均为 0；生成器只按完整稳定内部备注定位受管订单，未删除、重建、截断或修改无法确认归属的数据。本轮长期数据净增 180 条。最新质量报告 `SKYROC-AUTOTEST-20260713090203-BEF074763-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、41 张非空表、1,835 条长期记录，`sale_order=60`、`sale_order_detail=120`、`order_audit_log=110`；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和十项一致性检查失败均为 0。整体仍有 41 项数量/业务链路缺口，故 T2 保持未勾选。订单层 RED 用例先按预期失败（期望 60、实际 0），GREEN 后通过；`DemoData|DataQuality` 相关测试 23 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 448 项通过且 0 跳过（耗时 15 分 26 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续采购计划、采购单、库存、配送任务、售后、财务与溯源业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 补齐系统支撑长期联调数据：新增失败用例先确认受管服务时段为 0，再由 `DemoDataGenerator` 经 `ISystemSupportService` 幂等补齐 30 个运营服务时段、30 条通知公告和 2 条运营设置，经 `IPrintService` 创建 30 个受管打印模板及 90 个模板字段；登录日志和操作日志没有公开写入口，仅对完整稳定键命中的受管样本使用根事务内受控构造器补齐 120 条登录审计和 120 条操作审计。第二次生成各受管层新增均为 0，未删除、重建、截断或修改无法确认归属的数据；本轮长期数据净增 422 条。最新质量报告 `SKYROC-AUTOTEST-20260713080346-AEEDBF049-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、38 张非空表、1,545 条长期记录，`sys_service_period=30`、`sys_notice=30`、`sys_setting=2`、`print_template=30`、`print_template_field=90`、`sys_operation_log=124`、`sys_login_log=133`；孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和十项一致性检查失败均为 0。整体仍有 44 项数量/业务链路缺口，故 T2 保持未勾选。系统支撑专项 RED/GREEN 用例、`DemoData|DataQuality` 相关测试 22 项、真实 PostgreSQL 质量报告测试 2 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 447 项通过且 0 跳过（耗时 11 分 50 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续订单、采购、库存、配送任务、售后、财务与溯源业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `IDepartmentService` 补齐 `SKYROC-DEMO-DEPARTMENT-001` 至 `030`：6 个根部门与 24 个子部门形成受管组织树，首次新增 30 条、第二次新增 0 条。每条受管部门均填写负责人、负责人姓名、手机号、企业邮箱、排序、业务备注、启用状态及创建审计字段；既有部门代码校验已同步允许设计规定的稳定键连字符。生成器只按完整稳定编码查询/更新受管部门，未修改两条既有非受管部门或其他长期数据；系统用户与采购员均改为关联受管部门集合。质量报告 `SKYROC-AUTOTEST-20260713072646-50511737B-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、33 张非空表、1,123 条长期记录，`sys_department` 为 32 条（受管 30 条的断言字段为 100% 填充；两条非受管既有记录保持原状，因此全表部分可空/审计字段不满 100%）。孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和十项一致性检查失败均为 0；整体仍有 50 项数量/业务链路缺口，故 T2 保持未勾选。部门专项和真实 PostgreSQL 元数据报告测试 3 项通过；`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 446 项通过且 0 跳过（耗时 5 分 45 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续订单、采购、库存、配送、售后、财务、溯源及系统支撑的稳定联调数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `IRoleService` 与 `IUserService` 补齐 `SKYROC-DEMO-SYSTEM-ROLE-001` 至 `030` 和 `SKYROC-DEMO-SYSTEM-USER-001` 至 `030`，并为每名受管用户精确绑定同序号角色及四项既有系统菜单。首次新增角色、用户、用户角色关系各 30 条和角色菜单关系 120 条，第二次角色与用户新增均为 0；用户部门、角色菜单和用户角色关系因现有公开 DTO/服务事务边界未能在根事务中完整表达，仅对完整稳定键命中的受管记录受控补齐，未修改非受管长期数据。质量报告 `SKYROC-AUTOTEST-20260713071037-BE9FF1B85-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、33 张非空表、1,093 条长期记录，`sys_user`、`sys_role`、`sys_user_role` 分别为 32 条，`sys_role_menu` 为 175 条；受管用户的部门、账号、昵称、电话、邮箱、密码哈希及状态，以及受管角色的编码、名称、描述及状态均为 100% 填充。孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留和十项一致性检查失败均为 0；整体仍有 51 项数量/业务链路缺口，故 T2 保持未勾选。新增权限资料与元数据质量相关真实 PostgreSQL 测试 2 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 445 项通过且 0 跳过（耗时 6 分钟）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续部门、日志及订单采购库存配送售后财务溯源等剩余联调数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `ICustomerSubAccountService` 补齐 `SKYROC-DEMO-CUSTOMER-SUB-ACCOUNT-001` 至 `030`：首次新增 30 条受管客户子账号，第二次运行新增 0 条。每条账号精确关联同序号受管公司和客户，并填写昵称、手机号、企业邮箱、不可逆摘要形式的密码哈希、业务备注、启用状态与创建审计字段；生成器仅按完整稳定账号定位和更新受管记录，未修改任何非受管长期数据。质量报告 `SKYROC-AUTOTEST-20260713064945-5237370AA-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、883 条长期记录，`customer_sub_account` 为 30 条，账号、昵称、电话、邮箱、密码哈希和备注字段填充率均为 100%，孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留均为 0，十项一致性检查均通过；整体仍有 54 项数量/业务链路缺口，故 T2 保持未勾选。客户子账号与元数据质量相关真实 PostgreSQL 测试 2 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 444 项通过且 0 跳过（耗时 5 分 42 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续剩余系统权限、基础资料与业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `ICarrierService`、`IDriverService` 和 `IDeliveryRouteService` 补齐 `SKYROC-DEMO-CARRIER-001` 至 `030`、`SKYROC-DEMO-DRIVER-001` 至 `030`、`SKYROC-DEMO-DELIVERY-ROUTE-001` 至 `030`，以及每条路线精确对应的一条受管客户关系。首次新增承运商、司机、路线、客户路线关系各 30 条；第二次运行四类记录均新增 0。承运商、司机、路线及关系的全部当前适用业务/创建审计字段均为 100% 填充，司机均绑定受管承运商，路线均绑定受管客户；路线关系排序和创建审计因公开服务未暴露写入口，仅对完整稳定键定位的受管关系受控补齐，未修改非受管长期数据。质量报告 `SKYROC-AUTOTEST-20260713061906-7A57B0B2A-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、853 条长期记录，`carrier`、`driver`、`delivery_route`、`customer_route` 各 30 条，孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留均为 0，十项一致性检查均通过；整体仍有 55 项数量/业务链路缺口，故 T2 保持未勾选。配送层真实 PostgreSQL 测试 1 项、生成器与质量报告相关真实 PostgreSQL 回归 13 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 443 项通过且 0 跳过（耗时 5 分 55 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次仍严格领取 T2，继续剩余系统权限、基础资料与业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”仍未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `IPurchaseRuleService` 补齐 `SKYROC-DEMO-PURCHASE-RULE-001` 至 `030`：首次新增 30 条采购规则、30 条规则商品关系和 30 条规则客户关系，第二次规则新增为 0。每条受管规则均精确关联受管供应商、采购员、仓库、商品分类、商品和客户，填写名称、启用状态、采购模式与业务备注；采购模式覆盖供应商直供 15 条、市场自采 15 条，未修改非受管长期数据。质量报告 `SKYROC-AUTOTEST-20260713060430-492FACD68-quality.json` 及对应 Markdown 显示 `neondb` 为 80 张业务表、28 张非空表、733 条长期记录，采购规则主表及两类关系表各 30 条，规则适用业务字段填充率均为 100%，孤儿外键、重复业务编码、`SKYROC-AUTOTEST-*` 临时残留均为 0，十项一致性检查均通过。采购规则专项真实 PostgreSQL 测试 1 项、质量报告测试 1 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误、`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 442 项通过且 0 跳过（耗时 4 分 9 秒）和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；此前 184 秒窗口仅因执行时限过短而中止，后续长窗口已完成验证。整库仍存在未补齐的数量与业务链路，因此 T2 保持未勾选；下一次仍严格领取 T2，继续剩余权限、基础资料与业务链路数据层。**

**自动业务测试断点：T2“全量联调数据生成器”尚未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `ICustomerProtocolService` 与 `ICustomerProtocolGoodsService` 补齐 `SKYROC-DEMO-CUSTOMER-PROTOCOL-001` 至 `030`：首次新增 30 条客户协议价、30 条协议商品和 30 条客户协议关系，第二次协议价与协议商品新增均为 0。每条受管协议均精确关联受管报价、客户、商品和基础单位，填写名称、有效期、备注、协议单价、最小起订量、创建审计字段；价格与数量均按 `NumericPrecision` 生成，状态分布为启用 30 条。生成器只按完整稳定编码查询并在同一事务中写入，未修改非受管长期数据。质量报告 `SKYROC-AUTOTEST-20260713041252-32E7145E0-quality.json` 及对应 Markdown 显示 `neondb` 为 80 张业务表、25 张非空表、643 条长期记录，协议主表及两类关系表各 30 条，相关适用字段填充率均为 100%，孤儿外键、重复业务编码和 `SKYROC-AUTOTEST-*` 临时残留均为 0，十项一致性检查均通过；整体质量验收仍有 62 项数量/链路缺口，因此 T2 不能勾选。真实 PostgreSQL 相关测试 17 项、全量测试 441 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过。下一次严格继续领取 T2 的采购规则及其余权限与基础资料层。**

**自动业务测试断点：T2“全量联调数据生成器”尚未勾选。本轮以真实 PostgreSQL RED/GREEN TDD 经 `ISupplierService` 补齐 `SKYROC-DEMO-SUPPLIER-001` 至 `030`：首次新增 30 条、第二次零新增；每条受管供应商均填写名称、联系人、电话、地址、开户行、银行账号、税号、备注和创建审计字段。当前基线为 80 张业务表、17 张非空表、343 条长期记录，`supplier` 共 31 条（30 条受管记录均完整）；本轮报告 `SKYROC-AUTOTEST-20260713024447-EEB2A0AFF-quality.json` 及对应 Markdown 显示孤儿外键、重复业务编码和临时数据残留均为 0，十项一致性检查均通过，但整体质量验收仍有 71 项数量/链路缺口，且一条既有非受管供应商使全表供应商联系、开户、税务与备注字段填充率为 96.7742%，未作任何无法确认归属的修改，因此 T2 不能勾选。真实 PostgreSQL 相关测试 8 项、全量测试 437 项、构建 0 警告/错误和格式检查均通过。阻塞原因仍是未实现其余权限、基础资料、定价、订单采购库存、配送售后财务溯源及系统支撑层的稳定生成；下一次严格继续领取 T2。**

T2 于 2026-07-13 继续以真实 PostgreSQL RED/GREEN TDD 补齐采购员资料层：先新增失败用例验证 `SKYROC-DEMO-PURCHASER-001` 至 `030` 尚未生成，再由 `DemoDataGenerator` 在既有白名单守卫、受控审计身份和同一事务中调用 `IPurchaserService` 创建或精确更新 30 条稳定采购员资料。每条均填写名称、联系电话、备注、创建审计字段，并关联既有系统用户和部门；生成器只按完整稳定编码查询，未修改任何既有用户、部门或其他非受管记录。首次补齐 30 条、第二次新增 0 条，采购员编码、名称、电话、用户、部门、备注和创建审计字段填充率均为 100%，状态分布为启用 30 条。质量报告 `SKYROC-AUTOTEST-20260713030014-B104557F9-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、18 张非空表、373 条长期记录，孤儿外键、重复业务编码和 `SKYROC-AUTOTEST-*` 临时残留均为 0；整体长期联调验收仍有 70 项数量/链路缺口，故 T2 保持未勾选。真实 PostgreSQL 相关测试 13 项、全量测试 438 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次严格继续 T2 的权限与其余基础资料层。

T2 于 2026-07-13 继续以真实 PostgreSQL RED/GREEN TDD 补齐商品与基础单位资料层：新增失败用例确认 `SKYROC-DEMO-GOODS-001` 至 `030` 不存在，再由 `DemoDataGenerator` 在既有白名单守卫、受控审计身份和同一事务中通过 `IGoodsService` 创建或精确更新 30 条稳定商品，并通过 `IGoodsUnitService` 创建或精确更新对应的 30 条基础单位。每条商品均精确关联受管商品分类、供应商和仓库，填充规格、品牌、产地、说明、税率、备注、创建审计与供货关系；每条单位均填充编码、名称、换算率、排序、备注、创建审计并回写商品基础单位。首次生成后第二次运行商品和单位新增均为 0；受管商品/单位的全部断言字段为 100% 填充，受管状态均为启用。质量报告 `SKYROC-AUTOTEST-20260713032104-57897A711-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、19 张非空表、463 条长期记录，商品 34 条、商品单位 30 条、商品供货关系 32 条；孤儿外键、重复业务编码和 `SKYROC-AUTOTEST-*` 临时残留均为 0，十项一致性检查均通过。整库长期联调验收仍有 68 项数量/链路缺口（包括权限、其余基础资料、定价、订单采购库存、配送售后财务溯源与系统支撑层），故 T2 保持未勾选。真实 PostgreSQL 相关测试 9 项、全量测试 439 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次严格继续 T2 的权限与其余基础资料层。

T2 于 2026-07-13 继续以真实 PostgreSQL RED/GREEN TDD 补齐报价资料层：先新增失败用例确认 `SKYROC-DEMO-QUOTATION-001` 至 `030` 尚未生成，再由 `DemoDataGenerator` 在既有白名单守卫、受控审计身份和同一事务中通过 `IQuotationService` 与 `IQuotationGoodsService` 创建或精确更新 30 条稳定报价、30 条报价商品和 30 条客户报价关系。每条报价均填写名称、描述、有效期、审核状态与创建审计；每条报价商品均填写单价、最小起订量、上架状态、备注与创建审计；关系的默认标记和有效期因现有服务未暴露写入口，仅按完整稳定键精确补齐。首次生成新增 90 条、第二次生成报价和报价商品均为 0；报价审核状态覆盖已审核 24 条、未审核 6 条，所有报价主表、明细和关系适用字段为 100% 填充（未审核客户的默认报价为空符合业务语义）。质量报告 `SKYROC-AUTOTEST-20260713035129-5BD486379-quality.json` 与对应 Markdown 显示 `neondb` 为 80 张业务表、22 张非空表、553 条长期记录，`quotation`、`quotation_goods`、`customer_quotation` 均为 30 条；孤儿外键、重复业务编码和 `SKYROC-AUTOTEST-*` 临时残留均为 0，十项一致性检查均通过。整体长期联调验收仍有 65 项数量/链路缺口，故 T2 保持未勾选。真实 PostgreSQL 相关测试 15 项、全量测试 440 项、`dotnet build SkyRoc.sln --no-restore` 0 警告/错误和 `dotnet format SkyRoc.sln --verify-no-changes` 均通过；下一次严格继续 T2 的协议价、采购规则及其余权限与基础资料层。

T0 已建立真实 PostgreSQL 测试宿主、精确数据库白名单、迁移入口、事务回滚夹具、唯一批次上下文、按主键与归属值双重限定的外键逆序清理器、JSON/Markdown 报告目录和中文使用说明。项目维护者已明确确认 `neondb` 是 SkyRoc 专用测试数据库；宿主仍强制 `Testing` 环境且实际库名必须精确匹配白名单。设计时 DbContext 工厂已移除内嵌远端连接，迁移必须显式提供 `ConnectionStrings__DefaultConnection`。真实数据库探针覆盖业务异常回滚、跨连接父子数据清理、重复清理幂等、归属不匹配拒绝、外键引用拒绝和 Web 宿主目标核对；所有探针均使用本轮 `SKYROC-AUTOTEST-*` 批次并已清零，未修改长期联调数据。

T0 数据库基线：80 张业务表、116 条记录，其中 10 张非空、70 张空表；扫描 1,408 个持久化字段和 102 个状态字段，平均通用填充率 96.15%，1,348 个字段当前为 100% 填充。基础质量报告未发现孤儿外键、重复业务编码或自动测试临时残留，迁移历史、外键验证和库存非负检查通过。当前数量和字段适用性尚未达到最终联调目标，按任务顺序由 T1 建立统一规则、T2 幂等补齐长期联调数据，本次未越界生成长期数据。受保护的一键入口成功确认数据库已是最新迁移；相关测试通过 19 项，全量 `dotnet build SkyRoc.sln --no-restore` 成功且 0 警告，`dotnet test SkyRoc.Tests/SkyRoc.Tests.csproj --no-build` 通过 422 项且 0 跳过，`dotnet format SkyRoc.sln --verify-no-changes` 和模型漂移检查通过。验收日期为 2026-07-12。

T1 已完成设计时 EF Core 模型与 PostgreSQL 目录的双向盘点，报告包含全表/列、可空性、数值精度、表列注释、外键、唯一索引、数量分类和字段适用性清单；`sys_setting` 单例约束及 `sys_login_log.username` 追加审计例外均集中登记并写入 JSON/Markdown 报告。七个非 PostgreSQL `WebApplicationFactory` 已使用命名 InMemory DbContext 和无副作用的登录/操作审计服务，真实 PostgreSQL 宿主保持真实实现。2026-07-12 验收报告为 80 张业务表、133 条记录、1,408 个持久化字段、102 个状态字段，平均字段填充率 95.23%，其中 1,335 个字段为 100% 填充；元数据问题、孤儿外键、重复业务编码和 `SKYROC-AUTOTEST-*` 残留均为 0，十项模型/迁移/外键/库存一致性门禁全部通过。`sys_login_log` 13 条和 `sys_operation_log` 4 条既有无批次审计记录保持不变，未被测试删除或新增。移除受跟踪默认 JWT 签名密钥后，部署配置门禁通过；真实 PostgreSQL 专项 21 项、全量测试 426 项、构建 0 警告和格式检查均通过。测试数据库基线为 80 张表、12 张非空表、68 张空表及 133 条长期联调记录；验收日期为 2026-07-12。

T2 已建立实施计划 `docs/superpowers/plans/2026-07-12-t2-demo-data-generation.md`，并以真实 PostgreSQL TDD 完成首个可执行公司层：`DemoDataGenerator` 经过数据库白名单守卫后，在同一层事务中调用 `ICompanyService` 创建或更新 30 条完整稳定键公司资料；为保证创建审计完整，生成器使用既有系统用户建立受控审计上下文，并仅对完整稳定键命中的受管公司修复缺失创建审计。首次生成新增 30 条，第二次生成零新增；未删除、截断、修改或按名称匹配任何非受管长期数据。最终质量报告 `SKYROC-AUTOTEST-20260712153838-62A524DEE` 显示 `neondb` 为 80 张业务表、13 张非空表、163 条长期记录，`company` 为 30 条且业务/创建审计字段填充率 100%，临时残留、孤儿外键和重复业务编码均为 0；但仍有 76 项数量/链路缺口（首项为售后、承运商、客户和财务空表），长期联调数据验收未通过。真实 PostgreSQL 专项 10 项、全量测试 432 项、构建 0 警告/错误和格式检查均通过。阻塞原因是 T2 尚未实现权限与其余基础资料、定价、订单采购库存、配送售后财务溯源及系统支撑层，必须继续 T2，严禁跳项。

T2 于 2026-07-13 继续以真实 PostgreSQL TDD 补齐客户标签层：先新增失败用例验证 `SKYROC-DEMO-CUSTOMER-TAG-001` 至 `030` 不存在，再由 `DemoDataGenerator` 在既有白名单守卫、受控审计身份和同一事务中调用 `ICustomerTagService` 创建或精确更新 30 条稳定客户标签。首次新增 30 条、第二次零新增；标签编码、名称、备注及创建审计字段填充率均为 100%，且不修改非完整稳定键命中的记录。质量报告 `SKYROC-AUTOTEST-20260712160948-7396FD0C4-quality.json` 与 Markdown 对应文件显示 `neondb` 为 80 张业务表、14 张非空表、193 条长期记录，孤儿外键、重复业务编码和 `SKYROC-AUTOTEST-*` 临时残留均为 0；长期联调验收仍因 75 项未补齐数量/链路缺口失败，T2 保持未勾选。真实 PostgreSQL 相关测试 10 项、全量测试 433 项、构建 0 警告/错误和格式检查均通过；下次必须继续 T2 的权限与其余基础资料层，严禁跳项。

已经完成到：分层架构、JWT 登录/刷新/注销、动态路由、用户/角色/菜单/按钮/部门、个人中心与修改密码，以及商品、客户、供应商、采购员、仓库、报价、协议价、采购规则的数据库模型和基础 API；权限基础设施已具备稳定权限码、JWT 权限声明、授权要求和处理器，系统管理及商品、客户、定价、采购、仓库基础资料接口均已接入细粒度授权；第一阶段基础资料已有服务级回归测试，Swagger 能区分匿名/受保护操作并展示权限码；订单阶段已新增销售订单、订单明细、审核记录实体、EF 配置和可回滚迁移，补齐订单/明细/审核 DTO、双查询模型、映射、验证和仓储，并已实现事务化订单分页、详情、创建、编辑、删除以及提交、通过、驳回、重提状态机；订单接口现已接入读、建、改、删、审核五类细粒度权限，Swagger 展示对应权限码，HTTP 脚本记录了创建到审核、删除的主流程，API 集成测试覆盖认证、授权、路由、序列化和完整状态流转；采购阶段已新增采购计划、计划明细和订单来源关联实体、EF 配置和可回滚迁移，模型支持采购模式、采购单生成状态、供应商/采购员快照、商品/采购单位快照和合并拆分所需的来源数量关系；采购计划 API 已落地查询、详情、手工新增和“从已审核订单生成计划”，手工新增校验采购单位归属并写入商品/单位/供应商/采购员快照，生成流程按商品聚合明细、保留订单来源关系并回写订单及订单明细的采购计划标记、拒绝未审核或已生成计划的订单；未发布计划现可批量分配或清除供应商/采购员，兼容计划可按商品和采购单位聚合合并并保留来源订单关系，拆分支持按来源订单和按商品采购数量两种方式且维持需求数量守恒，所有变更统一拒绝已生成采购单的计划；采购计划接口已接入读、建、改细粒度权限，Swagger 展示权限码，HTTP 脚本记录查询、生成、分配、合并和拆分主流程；采购单、采购单明细和计划关联模型及可回滚迁移已落地，保存供应商/采购员/联系人、商品/单位/价格快照以及计划占用数量；采购单 API 已实现分页、详情、手工新增、编辑、删除、完成和取消状态流转，并可将计划剩余数量按采购模式、供应商和采购员分组生成采购单；草稿编辑、删除或取消会原子回写计划已采购数量和生成状态，完成或取消后禁止继续修改；采购单接口复用采购读、建、改、删细粒度权限，服务和权限测试覆盖快照、金额、计划占用回写与非法状态流转；采购 Swagger 已覆盖计划与采购单路由、权限码、Bearer 认证及 401/403 响应，HTTP 脚本新增可自动传递业务 ID 的十步“订单 -> 计划 -> 采购单”主流程，共享 EF Core InMemory 测试宿主通过真实控制器、应用服务和仓储验证了订单采购标记、计划来源与数量占用、采购单来源关系和完成状态；库存阶段已新增库存批次、只追加库存流水、入库主单/明细、出库主单/明细、盘点主单/明细和六类状态/来源枚举，模型保留仓库、商品、单位、业务方、数量、价格和批次快照，明确数量与金额精度、批次唯一性、可用库存约束、来源追溯、单次反向流水和盘点调整幂等标记，并生成包含完整表列注释的可回滚迁移；入库流程已落地采购入库、其他入库和销售退货入库的事务化 CRUD、审核与反审核，审核时按仓库/商品/批次号定位或创建批次、按基础单位换算入库数量、以移动加权平均法更新单位成本并只追加正向流水，反审核逐条生成反向流水回滚批次数量并拒绝可用库存不足的回滚，入库接口复用库存读、建、改、删细粒度权限（审核/反审核归入更新权限）；出库流程已落地销售、采购退货和其他出库的事务化 CRUD、审核与反审核，出库商品行强制选择当前仓库的库存批次并按单位换算基础数量，审核在事务内锁定单据、来源销售订单和批次，校验订单剩余可出库数量与批次可用库存后原子扣减并追加负向流水，反审核追加反向正流水恢复库存，销售出库同时回写订单部分/全部出库状态；出库接口复用库存读、建、改、删细粒度权限（审核/反审核归入更新权限），HTTP 脚本覆盖三类出库的建、查、改、审核、反审核和删除示例，真实 HTTP 集成测试验证其他出库扣减与恢复；库存盘点已实现分页、详情、创建和审核 API，创建时按指定批次固化仓库、商品、基础单位、账面数量与单位成本快照并由服务端计算账实差异，审核在事务内按稳定顺序锁定盘点单和库存批次，拒绝盘点快照后的库存变动或盘亏侵占预留库存，按盘盈/盘亏方向原子更新当前及可用数量并只追加盘点调整流水，以审核状态、调整标记和生效流水三重阻止重复调整；盘点接口复用库存读、建、改细粒度权限，HTTP 脚本覆盖查、建、审主流程；库存只读查询已实现按仓库和商品聚合的总览、含成本与效期的批次分页、保留审核与反审核记录的台账分页，总览在数据库侧计算数量、可用量、占用量、加权单位成本和货值，三个接口统一接入库存读取权限并由真实 HTTP 与 Swagger 测试覆盖。质量收口已为 55 张数据库表和 960 个持久化字段补齐可落库注释，为实体、仓储、服务和控制器公共契约补齐 XML 文档，并增加数据库注释及 XML 文档覆盖测试防止回退。当前共记录 345 个 HTTP 请求。

提交前评审已补强入库业务不变量：反审核会按原入库流水恢复移动加权成本；关联采购单时校验完成状态、业务方、采购模式、明细归属、商品和剩余可入库数量；审核与反审核在事务内锁定单据、库存批次并重新检查状态，采购入库审核同时锁定来源采购单以串行校验剩余数量。

P2-14 代码评审已补强出库并发一致性：编辑和删除会在事务内锁定单据并重新校验状态，避免与审核交错后覆盖或删除已生效单据；销售出库反审核会按剩余有效出库单重新计算订单最近出库时间；HTTP 删除示例改用独立草稿，可按顺序执行。

P2-15 代码评审已修复盘点最小数量精度边界：审核仅跳过舍入后严格为零的差异，盘亏会按六位精度后的余额直接拒绝任何负数，快照一致性改为精确比较账面数量；盘点创建改为一次批量读取全部批次，避免逐行数据库查询，并补充对应回归测试。最终复审结论为 approved。

P2-16 代码评审已补齐库存查询仓储实现的职责注释，并新增控制器三个查询操作统一绑定库存读取权限的结构化回归测试；后续一致性复核将普通字段与原始值映射统一迁移到 AutoMapper Profile，服务层仅保留查询与分页编排，占用量、批次货值、加权成本和台账带方向数量改为 DTO 只读计算属性；数量 6 位、金额 4 位和远离零舍入模式统一收口到 Shared 全局数值精度定义，订单、采购及库存计算不再保留硬编码精度，并补充映射配置、计算口径、JSON 往返和全局精度测试。最终复审未发现 blocker、warning 或 nit，结论为 approved。

P2-17 配送基础模型已落地承运商、司机、配送路线、客户路线关系和配送异常五个实体、EF 配置及带完整表列注释的可回滚迁移：承运商、司机、路线按名称编码基础资料实现分页、详情、增删改、批量删除和启停状态 CRUD 及细粒度权限，司机可绑定承运商并在创建/更新时校验承运商存在，路线支持初始分配客户及“分配客户路线”整体替换接口；客户路线关系以（路线、客户）唯一约束落库，路线删除级联清理客户关系；配送异常保存唯一异常编号、处理状态默认待处理、司机/客户可空引用，已提供仓储与编号唯一性判断供 P2-18 异常登记接入。配送接口统一接入新增的 `business:delivery` 读、建、改、删权限码，控制器权限映射回归测试已同步覆盖三个控制器和路线客户分配操作。

P2-17 代码评审修复了一处正确性缺陷：基础资料创建/更新映射在 `Status` 传空时会把实体默认启用状态覆盖为无效的 0，配送创建/更新映射改为空值回退到目标实体现有状态（新建保持启用、编辑不覆盖当前状态），并补充创建保持启用与更新保留禁用状态的回归测试；同时将“分配客户路线”的响应改为直接以规范化后的目标客户集合构造，避免复用删除后失效的跟踪导航在真实数据库返回过期客户。最终复审结论为 approved。

P2-18 配送任务已实现从已审核销售出库幂等生成任务，每张出库单通过唯一约束只能生成一条任务，并固化销售订单、客户、联系人、收货地址、仓库及出库时间快照；新增订单任务、司机任务和详情查询，批量司机分配会锁定并校验启用司机、固化司机及承运商快照，智能规划按客户已配置的启用路线、路线排序和客户路线内顺序确定性匹配；配送异常登记从任务派生司机和客户关系，原子写入异常并把任务标记为异常状态。配送任务和异常 API 使用配送读、建、改权限，Swagger 与 9 个 HTTP 示例已同步。

P2-18 代码评审初审结论为 changes requested：已生成配送任务的销售出库仍可反审核，司机启用状态在事务外读取存在并发竞态，批量操作无上限且更新后逐条回读存在 N+1 查询，显式空任务集合可能触发校验器空引用，删除已分配司机会留下“已分配但无司机”的任务。以上问题已通过出库反审核保护、司机行锁、单次 100 条限制、批量聚合回读、停止级联校验，以及司机任务引用删除保护和数据库限制外键修复并补充回归测试；最终复审 blocker 0、warning 0、nit 0，代码评审 approved，安全评审 pass。

P2-19 签收回单已实现“已分配 -> 配送中 -> 已签收”状态机，异常登记进入异常状态、处理全部异常后按是否已开始配送恢复执行状态；新增签收回单和商品验收明细模型、完整表列注释及可回滚迁移，签收请求必须完整覆盖本次销售出库行，验收金额按全局数量/金额精度由服务端计算。任一任务开始即同步订单配送中，只有全部有效销售出库均已生成任务且全部签收才同步整单已签收和结算金额，全部回单归档后才同步已回单。签收、回单、异常处理 API 已接入配送更新权限，Swagger、HTTP 和模型/服务/权限回归测试已同步。

P2-19 代码评审初审结论为 changes requested：签收明细 100 条上限会阻塞合法大单，聚合仓储更新会把整张导航图标记修改，回单明细响应顺序不稳定。以上问题已通过取消与完整覆盖规则冲突的数量上限、仓储根实体定向更新和稳定排序修复并补充回归测试；复审 blocker 0、warning 0、nit 0，结论 approved。

P2-20 第二阶段联调已新增真实 HTTP 完整链路测试，使用同一 InMemory 数据库和实际控制器、应用服务、仓储贯通“订单创建/审核 -> 采购计划 -> 采购单完成 -> 采购入库审核 -> 销售出库审核 -> 配送任务/司机分配 -> 开始配送 -> 客户签收 -> 回单归档”。测试逐段核验订单、计划、采购单、出入库、配送和回单来源主键连续性，验证入库增加库存、出库扣减库存及两条只追加流水，并覆盖客户短收后的验收状态、结算金额和整单回单状态。任务没有新增业务模型、数据库结构或 API，因此无需迁移或新增 HTTP 示例；第二阶段至此完成。

P3-01 售后模型已新增售后单、售后商品、售后审核记录和按商品生成的取货任务四个实体及状态、类型、原因、处理方式枚举；模型保存订单、客户、商品、单位、供应商、责任部门、司机和联系地址快照，统一采用全局数量与金额精度。数据库通过售后编号、取货任务编号和商品来源唯一约束防止重复记录，通过状态、枚举、非负金额、正数量与换算率检查约束阻止非法持久化；取货任务同时关联售后单和售后商品，并以复合外键保证二者归属一致。迁移为 4 张新表和 91 个字段写入完整表列注释，按依赖顺序可回滚；7 项模型测试覆盖映射、文档状态值、默认值、精度、检查约束、唯一约束和删除行为。本项没有新增 API，因此无需修改 Swagger 或 `SkyRoc.http`。

P3-02 售后流程已实现分页、详情、创建、编辑、删除未提交草稿，以及首次提交、驳回、重提、审核通过、反审核和处理完成状态机。来源订单在事务内加锁，商品、单位、客户、供应商和责任部门批量校验并固化快照；订单商品按已验收数量或订单数量控制累计售后占用，防止并发超退。审核按申请类型和处理方式将纯退款送入待退款，将退货、补货和换货送入实物处理；补货、换货不误减客户结算金额，金额和数量统一使用全局精度。审核轨迹只追加，已生成取货任务禁止反审核，未完成取货任务禁止直接完成。新增独立售后读、建、改、删、审核权限，Swagger 和 13 个 HTTP 示例覆盖完整主流程。

P3-02 代码评审初审结论为 changes requested：补货/换货错误计入退款金额，DTO 汇总金额未使用全局精度，极小正数量舍入后可能违反数据库正数约束，草稿替换商品行在 PostgreSQL 唯一约束下存在插入先于删除风险，审核说明长度未统一校验。以上问题已通过处理方式分流、全局精度收口、舍入后正数校验、事务内先删除落库再插入及统一长度校验修复，并补充回归测试；复审 blocker 0、warning 0、nit 0，结论 approved。

P3-03 售后库存联动已实现退货退款商品审核时逐行幂等生成取货任务，并新增任务查询、司机分配、开始和完成状态机。销售退货入库现可绑定售后单与已完成取货任务，按客户、商品、单位、批准数量和价格快照校验来源，通过取货任务唯一索引和事务内稳定批量锁防止并发重复入库；完全相同请求返回原单，来源相同但内容漂移时拒绝。售后完成要求全部取货任务完成且对应退货入库审核通过，售后完成与退货入库反审核统一使用“售后单 -> 入库单”锁顺序，已完成售后禁止回滚库存。新增取货任务权限、Swagger、9 个 HTTP 示例和跨服务集成测试，退货库存流水与批次数量均已验证。

P3-03 代码评审初审结论为 changes requested：取货完成后可在退货尚未入库时直接完成售后，售后完成后仍可反审核退货入库，并发执行可能产生状态与库存不一致；售后完成逐单锁定入库单存在 N+1。以上问题已通过审核入库完成门槛、统一锁顺序、完成后反审核保护和稳定批量锁修复，并补充回归测试；复审 blocker 0、warning 0、nit 0，结论 approved。

P3-04 客户账单模型已新增客户账单和客户账单明细实体、状态枚举、来源类型枚举、EF 配置及可回滚迁移，按订单维度汇总签收应收、售后冲减、已结金额和账单状态；账单明细保存订单商品验收或售后商品调整的来源单据、来源明细、商品/单位快照、数量、单价、金额和业务时间，通过（来源类型、来源明细）唯一约束保证同一订单商品或售后商品只入账一次。配送签收在整单全部配送任务签收并同步订单结算金额后幂等生成客户账单正向应收；售后完成在来源订单已签收时追加负向调整，若售后先完成则由后续订单签收入口回捞已完成售后并一并冲减。新增客户账单仓储和同步服务，签收及售后完成路径均在既有事务内更新账单，未新增控制器或 HTTP 示例。模型、签收生成、售后调整和跨顺序回归测试已覆盖。

P3-04 代码评审初审结论为 changes requested：通用字段注释覆盖了库存流水 `source_type/source_detail_id` 注释并产生无关迁移，已改为实体级注释并重生成迁移；已存在账单追加新明细时 EF InMemory 将明细误判为修改，已通过仓储显式标记新增明细修复；复审 blocker 0、warning 0、nit 0，结论 approved。

P3-05 客户结款已新增客户结款凭证和凭证明细实体、状态枚举、EF 配置及可回滚迁移，记录客户付款流水、优惠金额、核销金额、剩余金额、作废审计和账单/订单快照；数据库约束覆盖状态、非负金额、明细唯一账单、来源外键和金额精度，表列注释完整可落库。客户结款 API 已实现待结账单分页、凭证分页、详情、创建和作废：创建时锁定所选客户账单，校验同一客户、重复账单、余额上限和部分/全部结款状态，按收款与优惠合计回写账单已结金额和状态；作废时锁定凭证与账单并按明细核销金额反向回滚，防止重复作废。新增财务权限 `business:finance:*`、Swagger 财务标签、5 个 HTTP 示例和模型/服务/权限/Swagger 回归测试。

P3-05 代码评审初审结论为 changes requested：创建结款请求在 `Details = null` 时可能触发校验器空引用，已改为 null-safe 上限校验并补充回归测试；复审逐项检查正确性、边界值、并发一致性、安全性、性能、分层、注释、迁移和测试覆盖，blocker 0、warning 0、nit 0，结论 approved。

P3-06 供应商结算已新增供应商待结单据、待结明细、结算单及结算明细实体、状态/来源枚举、EF 配置及可回滚迁移，按采购入库单或采购退货出库单聚合应付金额，保存供应商与出入库快照，并通过来源明细唯一约束保证幂等；采购入库/退货出库审核在既有事务内同步待结单据，反审核在无有效结算时删除待结单据并拒绝已有结算金额的来源回滚。供应商结算 API 已实现待结单据分页、结算单分页、详情、创建和作废：创建时锁定所选待结单据，校验同一供应商、重复单据、余额上限和部分/全部结款状态，按付款与优惠合计回写已结金额和状态；作废时锁定结算单与待结单据并按明细核销金额反向回滚。复用财务权限 `business:finance:*`、Swagger 财务标签、5 个 HTTP 示例和模型/同步/服务/权限/Swagger 回归测试。

P3-06 代码评审初审结论为 changes requested：通用字段注释误改 `stock_in_detail`/`stock_out_detail` 外键列语义，已补充实体级注释并重生成无 AlterColumn 的迁移；缺少采购入库审核自动生成待结单据的集成回归，已补充 `StockInServiceTests` 审核/反审核联动测试。复审逐项检查正确性、边界值、并发一致性、安全性、性能、分层、注释、迁移和测试覆盖，blocker 0、warning 0、nit 0，结论 approved。

P3-06 二次代码评审修复了一处 PostgreSQL 外键违反缺陷：供应商结算单作废只回滚待结单据已结金额、保留结算明细（`supplier_settlement_detail` → `supplier_bill` 为 Restrict），此时反审核来源采购入库/退货单会因 `SettledAmount` 已归零而进入删除待结单据分支，触发原始外键异常（500）而非业务错误（InMemory 测试不校验外键故此前未暴露）。已新增 `ISupplierSettlementRepository.ExistsDetailByBillIdAsync`，`SupplierBillService` 反审核校验与删除路径在存在结算记录（含已作废）时抛出清晰业务异常，并补充 `SupplierBillServiceTests` 作废后反审核拒绝的回归测试。

P3-07 第三阶段联调新增 `FinanceAfterSaleFlowApiIntegrationTests` 真实 HTTP 完整链路测试，使用同一 InMemory 数据库和实际控制器、应用服务、仓储贯通两条第三阶段财务主链路：一是“订单创建/审核 -> 采购计划 -> 采购单完成 -> 采购入库审核 -> 销售出库审核 -> 配送任务/司机分配 -> 开始配送 -> 整单验收签收 -> 客户账单正向应收 -> 售后建单/提交/审核 -> 取货任务执行 -> 销售退货入库审核 -> 售后完成冲减客户账单 -> 客户结款部分/作废/全额”，逐段核验供应商待结单据金额、客户账单订单应收与售后冲减金额（27 冲减为 18）、结款凭证部分/全额与作废回滚状态、账单余额回写和数据库最终已结金额；二是“采购入库审核 -> 供应商待结单据 -> 供应商结算部分/作废/全额”，核验待结单据来源、余额和结算状态。第三阶段未新增业务模型、数据库结构或 API，售后与财务模块的 Swagger 契约、权限映射和 `SkyRoc.http` 示例已在 P3-02、P3-05、P3-06 落地并由 `SwaggerResponseSchemaTests`、`FinanceControllerPermissionTests`、`AfterSaleControllerPermissionTests` 覆盖，因此本任务无需迁移或新增 HTTP 示例；第三阶段至此完成。

P4-01 溯源模型已新增检测报告、报告商品、报告附件、溯源记录和外部报送日志五个实体，以及检测结论、附件类型、报送业务类型和报送状态四个枚举。检测报告绑定来源采购入库单并固化入库单号、仓库和供应商快照，报告商品按入库明细行固化商品、单位、分类和批次快照，同一报告内每行入库明细通过唯一约束只能登记一次，送检数量采用全局数量精度且必须为正；附件区分报告文件与现场图片并随报告级联删除，文件大小非负受约束保护。溯源记录以唯一溯源编号作为二维码对外标识，将销售订单商品行（每行最多一条溯源）与采购入库来源、供应商、仓库、批次和检测报告串联，业务快照均在生成时固化；外部报送日志只追加记录业务类型、来源编号快照、目标平台、脱敏请求/响应报文、错误摘要和重试次数，报送状态默认待报送，来源主键为多态引用不建数据库外键。迁移为 5 张新表写入完整表列注释并按依赖顺序可回滚；8 项模型测试覆盖表映射、枚举取值、默认值、精度、检查约束、唯一索引和删除行为。本项没有新增 API，因此无需修改 Swagger 或 `SkyRoc.http`。

P4-01 代码评审初审结论为 approved（blocker 0、warning 2、nit 4），全部可操作问题已在本次修复：溯源记录补充商品分类名称快照以支撑规格中“按商品分类查询”的历史口径；附件文件大小检查约束去除冗余的 `IS NULL` 分支；12 处复用共享登记的泛化列注释改为实体级精确注释（未触碰共享键，既有表注释零变更）；外部报送日志重试次数补充数据库默认值 0 并新增默认值断言；补充报送状态与平台两个查询索引的测试断言。迁移按规范流程移除后重建，仅含 CreateTable/CreateIndex。

P4-02 溯源流程已实现检测报告分页、详情、创建、编辑和删除，且仅允许已审核采购入库单作为来源、固化商品/单位/批次、仓库和供应商快照并校验送检数量；报告一旦被溯源引用即完全冻结，删除或编辑均返回业务错误。销售订单的已审核销售出库可生成缺失溯源记录，流程在事务内锁定订单和关联报告、复核报告商品关系并拒绝缺少或存在多个已审核采购入库来源的批次，避免错误来源、重复生成、报告删除/编辑竞态和外键异常。溯源列表支持订单、仓库、供应商、商品、分类和关键字查询；二维码详情为匿名白名单，仅返回无内部主键、订单或客户信息的公开商品与检测报告快照；外部报送日志提供业务类型、平台、状态和时间筛选，接口不返回请求/响应报文。新增 `business:traceability:*` 权限、溯源 Swagger 标签与 8 条 HTTP 主流程示例，服务/权限/Swagger 回归测试覆盖报告快照、来源边界、引用保护、幂等生成、公开 DTO 和接口契约。本项未修改持久化模型，`dotnet ef migrations has-pending-model-changes` 确认无需新迁移。最终双轴代码评审 blocker 0、warning 0、nit 0，结论 approved。

库存查询性能优化：库存批次分页 `IStockBatchRepository.GetQueryPagedAsync` 原先 `Include` 仓库、商品、商品分类和基础单位四张完整表并返回 `StockBatch` 实体，数据库需回传各表全部列（含每张表 7 个 `BaseEntity` 审计列）再由 AutoMapper 拍平，实际仅使用约 17 个展示字段。现新增 `Domain/ReadModels/Storage/StockBatchReadModel`，仓储在分页后以 `.Select(...)` 在数据库侧投影为读模型，移除全部 `Include`，EF 仅按投影引用生成必要 JOIN 并只 `SELECT` 展示列；接口、`StockQueryService` 与 `StockQueryMappingProfile` 同步改为 `StockBatchReadModel -> StockBatchDto`（仅保留数量/成本精度舍入，关联名称已在投影拍平）。与既有 `GetOverviewPagedAsync` 投影风格统一。台账 `StockLedger` 因字段与 DTO 近 1:1 且快照均在本表、无 `Include`，投影收益有限，本次未改。映射回归测试同步更新为读模型构造，全量 307 项测试通过。

P4-03 销售与售后报表已落地只读汇总查询：新增报表读模型、仓储投影、应用服务和 `ReportsController`，按已签收销售订单的客户验收数量/金额汇总商品、商品分类、客户和区域；当前模型没有独立区域实体，区域口径明确采用销售订单配送地址快照。售后汇总按已完成售后商品的申请类型、原因和处理方式统计退款/减免数量与金额；补货、换货、客户沟通不计入退款减免数量。商品分类筛选按所选分类当前名称匹配订单明细分类名称快照，与分类汇总展示口径一致。验收数量/金额优先持久化 `CustomerCheck*`，缺失按 0，不回退下单量。接口统一使用 `business:report:read` 权限；admin（`*:*:*`）可直接访问，非 admin 业务角色需在系统菜单中配置 `business:report:read` 按钮并授权后才能调用（与其他业务模块相同，DbSeeder 不预置业务按钮）。Swagger 报表标签、5 个 HTTP 示例、服务聚合测试、权限测试和 Swagger 契约测试已同步。本项未修改持久化模型，`dotnet ef migrations has-pending-model-changes` 确认无需新迁移。代码评审已处理售后非财务数量口径、分类筛选与快照对齐、验收公式注释与权限启用说明。

P4-04 库存与采购报表已落地只读汇总查询：在既有报表边界内新增库存日出入库、日商品出入库、采购出入库商品汇总、供应商汇总和采购员汇总五类接口。库存报表仅统计已审核入库/出库单据，按入库时间或出库时间归属自然日，数量统一使用基础数量口径，金额使用单据明细金额；采购报表仅统计采购入库和采购退货出库，供应商维度使用出入库主单供应商快照，采购员维度的退货出库通过出库批次追溯原采购入库采购员并避免空批次误匹配。接口统一使用 `business:report:read` 权限，Swagger 报表标签、5 个 HTTP 示例、服务聚合测试、权限测试和 Swagger 契约测试已同步。本项未修改持久化模型，`dotnet ef migrations has-pending-model-changes` 确认无需新迁移。最终代码评审 blocker 0、warning 0、nit 0，结论 approved。

P4-05 首页驾驶舱已落地六项只读聚合接口：经营概览、按订单日期的销售趋势、客户销售排行、商品分类销售排行、客户对账与取货状态。销售指标统一仅统计已签收订单明细的客户验收金额；两类排行按金额降序并以名称稳定排序，`rankSize` 服务端限制为 1 至 100；对账按客户账单业务日期聚合净应收、已结和逐账单非负待结余额；取货状态按任务创建时间筛选，并对不存在的状态补零，方便固定展示。接口复用 `business:report:read` 权限，Swagger 首页驾驶舱标签、6 条 HTTP 示例、服务聚合、权限及 Swagger 契约测试已同步。本项未修改持久化模型，`dotnet ef migrations has-pending-model-changes` 确认无需新迁移。

P4-05 代码评审初审结论为 changes requested（轻量）：口径实现正确但回归测试未锁死“不回退到下单金额”、名称并列排序、`rankSize` 钳制、逐账单待结下限与全部取货状态补零；服务类 summary 未覆盖驾驶舱，Domain 读模型金额字段缺单位说明。已补强 `DashboardReportServiceTests`、更新 `IReportService`/`ReportService` 与读模型 XML 注释，并校准 `01-dashboard.md` 关键字段表。复审 blocker 0、warning 0、nit 0，结论 approved。

P4-06 导入导出任务已落地统一 `ImportExportJob` 模型和可回滚迁移：当前支持商品（`jobType=1`）UTF-8 BOM CSV 模板下载、整文件校验导入、商品导出和当前创建人任务状态查询。导入限制为 2 MiB，严格校验 CSV 引号、表头、税率精度、`true`/`false`、外键、字段长度及名称/编码重复；任一行失败不写入商品并持久化失败任务摘要。导出对 CSV 公式前缀进行中和，并返回任务主键响应头。迁移新增商品名称唯一索引以保证并发导入一致性，且对历史重复名称给出明确升级前置错误。权限、Swagger、HTTP 示例、数据库注释和 13 项专项回归测试已同步；最终严格评审 blocker 0、warning 0、nit 0，结论 approved。

P4-07 文件上传已新增统一 `StoredFile` 元数据模型及可回滚迁移，提供 `/api/files` 上传和创建人范围的受保护下载。服务只接受不超过 10 MiB 的 PDF、PNG、JPEG，并同时核对扩展名、声明 MIME 类型和二进制签名；读取过程硬性限流，随机存储键不暴露物理路径，配置位于 Web 根目录内会被拒绝。下载将越权、缺失元数据与物理文件缺失统一为未找到，写入元数据失败时会删除本次写入的文件。权限、Swagger、HTTP 示例、全局流程、数据库注释和专项回归测试已同步；代码评审补充修复 Windows 跨盘绝对目录误判、创建人数据库非空约束和文件响应机械映射问题，并新增对应回归测试，最终 blocker 0、warning 0、nit 0，结论 approved。

P4-08 打印能力已新增 `PrintTemplate`、`PrintTemplateField` 实体、完整表列注释和可回滚迁移；模板以唯一编码、业务类型、设计 JSON 和字段路径维护，停用模板不会按编码提供业务选择。`/api/print-data/{businessType}` 支持销售订单、采购单、入库单、出库单、客户结款和供应商结算快照，订单/入库/出库通过显式确认接口原子标记已打印。权限、Swagger、HTTP 示例、业务流程和 11 项打印专项回归测试已同步；严格复审修复准确数据库注释、金额精度、只读查询、输入边界和并发唯一冲突处理，最终 blocker 0、warning 0、nit 0，结论 approved。

P4-09 系统支撑已新增 `ServicePeriod`、`SystemSetting`、`Notice` 和 `LoginLog` 实体，包含完整表列注释、约束、索引及可回滚迁移。`/api/system-settings` 支持非跨日服务时段、小程序下单开关与 0–30 天提前下单限制、最多四位小数的非负分拣权重；`/api/notices` 支持纯文本草稿、发布、撤回和删除；`/api/logs` 支持操作及登录日志分页查询和大小写无关的摘要内容检索。认证授权成功后的关键写操作使用独立 DI/DbContext 作用域写入脱敏审计，避免异常请求的追踪变更被审计提交；匿名写接口不会进入操作审计，密码、令牌和常见密钥参数统一脱敏，异常响应与审计共用 HTTP 状态映射。登录成功、凭据失败及认证流程中途故障均通过单一出口记录安全摘要，审计故障不改变认证结果；Web 请求来源由宿主层提供，应用层不直接依赖 `HttpContext`。响应模型机械映射已迁移到 AutoMapper，审计文本裁剪规则已统一。权限、Swagger、HTTP 示例、业务流程和专项回归测试已同步；最新复审修复了敏感密钥泄露、匿名请求误审计、内容检索缺失、登录审计空洞及分层规范问题。

P4-10 已完成业务流程、Swagger、README、HTTP 示例计数和部署配置核对：按项目维护者决定恢复默认数据库连接，并将该连接的轮换与历史处理排除出本任务验收范围；JWT 密钥和开发种子密码保持为空，Development 默认不再开启种子；JWT 启动校验要求签名密钥至少包含 32 个 UTF-8 字节，测试宿主显式注入测试专用密钥。新增认证配置、部署配置和收口文档回归测试，全仓库格式基线已清理。本次未新增持久化模型、迁移、API 或 HTTP 示例。最终复审逐项检查正确性、边界值、并发一致性、安全性、性能、分层、注释、迁移和测试覆盖，blocker 0、warning 0、nit 0，结论 **approved**。

当前基线：`dotnet build SkyRoc.sln --no-restore` 成功且 0 警告，检测报告与溯源切片精确 FQN 测试 1 项及 PostgreSQL 质量/元数据精确测试 2 项通过，`dotnet format SkyRoc.sln --verify-no-changes` 通过；最近一次完整测试基线仍为 453 项通过、0 跳过、耗时 50 分 3 秒，本轮按每日聚焦策略未重跑完整测试项目。最新迁移为 `20260711121840_AddSystemSupport`，本轮未新增持久化模型。专用 PostgreSQL 测试库最新质量报告为 `SKYROC-AUTOTEST-20260714124432-B59163D94-quality.json`，当前基线为 80 张业务表、72 张非空表、4,515 条长期记录，自动测试临时残留、孤儿外键、重复业务编码、元数据问题和一致性失败均为 0，T2 尚余 21 项数量/链路缺口。`SkyRoc.http` 累计记录 378 个请求，本次未新增 API，因此无需新增 HTTP 示例。

## 模块盘点

| 业务模块 | 状态 | 当前实现 | 主要缺口 |
| --- | --- | --- | --- |
| 全局能力 | ✅ | 统一响应、分页、异常处理、Swagger、JWT、Redis/内存降级；CSV 导入导出、安全文件上传、打印、运营设置、通知公告、关键操作审计及登录日志查询已落地；基础资料、订单、采购计划、采购单、出入库、配送、签收、售后、客户结款、供应商结算、溯源、报表和首页驾驶舱共 378 个 HTTP 示例已记录；数据库表列及实体、仓储、服务、控制器公共契约注释已补齐并由测试防回退 | 无已知全项目收口缺口 |
| 登录与系统权限 | ✅ | 登录、刷新、注销、用户信息、动态路由、个人中心与修改密码；系统管理及商品、客户、定价、采购、仓库基础资料细粒度授权；运营设置、通知公告、关键操作审计和登录日志查询 | 无已知后端主链路缺口 |
| 商品与定价 | 🟡 | 商品分类、商品、单位、报价及明细、客户协议价及明细、审核和客户绑定；分类、单位、上下架、审核及定价关系已有服务级回归测试 | 学校/周期报价差异、批量操作 |
| 客户 | 🟡 | 公司、客户、标签、子账号及关系；天眼查资料补充；基础关联已有服务级回归测试 | 业务全流程联调和 API 集成测试 |
| 采购 | 🟡 | 供应商、采购员、采购规则及适用商品/客户关系；采购计划、采购单、各自明细及来源关系模型和迁移；采购计划查询、手工新增、从已审核订单生成、供应商/采购员分配、合并与两种拆分 API；采购单 CRUD、完成/取消、计划生成采购单及计划数量回写；采购接口已接入细粒度权限，采购入库已纳入第二阶段完整链路测试 | 采购协议价 |
| 库存 | 🟡 | 仓库基础资料及状态回归测试；库存批次、只追加流水、入出库主表/明细、盘点主表/明细模型及可回滚迁移；采购/其他/销售退货入库与销售/采购退货/其他出库 CRUD、审核和反审核；入库审核更新移动加权成本并增加库存，出库审核锁定并校验批次可用库存后扣减，反审核均追加反向流水；盘点创建固化账面与成本快照，审核校验快照有效性后原子执行盘盈盘亏并防重复调整；库存总览、批次与台账分页查询；库存接口细粒度权限、Swagger、HTTP 示例和专项测试已覆盖 | 期初库存维护、独立库存成本报表 |
| 首页驾驶舱 | ✅ | 概览、销售趋势、客户/商品分类排行、客户对账和取货状态只读聚合；已明确验收金额、账单业务日期和取货创建日期口径，并覆盖权限、Swagger、HTTP 与回归测试 | 无已知后端主链路缺口 |
| 订单 | ✅ | 销售订单、订单明细、审核记录、签收回单和商品验收明细模型；事务化 CRUD、审核状态机和轨迹；配送开始/签收/回单同步订单状态、验收数量、结算金额和客户账单正向应收；客户结款可回写账单余额；完整订单主链路及第三阶段财务/售后联调真实 HTTP 集成测试已覆盖；报表已按商品、分类、客户和配送地址快照汇总销售数据 | 无已知后端主链路缺口 |
| 售后 | 🟡 | 售后单、商品、审核记录和取货任务模型；分页、详情、草稿维护、提交、驳回、重提、审核、反审核和完成状态机；退货退款审核幂等生成取货任务，支持司机分配和取货执行，并与销售退货入库来源、批次库存和流水闭环；售后完成可冲减来源订单客户账单应收并进入客户结款；细粒度权限、Swagger 和 HTTP 示例已落地；第三阶段真实 HTTP 完整链路联调已覆盖售后建单到客户结款；报表已按申请类型、原因和处理方式汇总售后数据 | 更多分析维度与导出 |
| 配送 | 🟡 | 承运商、司机、配送路线基础资料 CRUD 与细粒度权限；从已审核销售出库幂等生成任务，支持司机分配、智能规划、配送执行、异常登记/处理、客户签收和回单归档；分批配送聚合及第二阶段完整链路测试已覆盖 | 配送单打印 |
| 财务 | 🟡 | 客户账单和账单明细模型已落地；订单签收生成正向应收，售后完成生成负向调整，支持售后先完成后签收时回捞调整；客户结款凭证及明细已落地，支持待结账单查询、部分/全部结款、优惠、作废和余额回写；供应商待结单据及结算单已落地，采购入库/退货审核同步应付，支持待结单据查询、部分/全部结款、优惠、作废和余额回写；模型、迁移、服务、权限和 Swagger 回归测试已覆盖；第三阶段真实 HTTP 完整链路联调已覆盖客户结款与供应商结算两条主链路 | 对账报表与导出 |
| 报表 | 🟡 | 已实现销售商品、分类、客户、区域汇总、售后汇总、库存日出入库、日商品出入库、采购出入库商品/供应商/采购员汇总及首页驾驶舱查询；接口接入 `business:report:read` 权限（非 admin 需配置菜单按钮），Swagger、HTTP 示例和回归测试已覆盖 | 导出 |
| 溯源 | 🟡 | 检测报告维护、订单商品溯源生成/查询、最小公开二维码详情和外部报送状态查询已落地；报告、订单与关联报告均受事务锁和来源完整性校验保护，Swagger、HTTP、权限和回归测试已覆盖，可复用全局安全文件上传地址作为附件 | 外部平台实际报送执行 |

## 分阶段工作模块

### P1 系统权限与基础资料（已完成）

- [x] P1-01 项目分层、EF Core、迁移、异常处理和 Swagger
- [x] P1-02 登录、Token 缓存、动态路由和权限基础数据
- [x] P1-03 商品、客户、供应商、采购员和仓库基础资料
- [x] P1-04 报价、协议价、采购规则及关联数据
- [x] P1-05 个人中心与密码
- [x] P1-06 细粒度授权基础设施
- [x] P1-07 系统接口授权
- [x] P1-08 基础资料授权
- [x] P1-09 商品定价回归测试
- [x] P1-10 客户采购基础回归测试
- [x] P1-11 第一阶段契约收口

### P2 订单主链路（已完成）

- [x] P2-01 销售订单、订单明细和审核记录
- [x] P2-02 订单 DTO、查询、映射、验证和仓储
- [x] P2-03 订单 CRUD
- [x] P2-04 订单审核状态机
- [x] P2-05 订单联调收口
- [x] P2-06 采购计划模型
- [x] P2-06A 全链路注释补全
- [x] P2-07 采购计划 API
- [x] P2-08 采购计划合并/拆分
- [x] P2-09 采购单模型
- [x] P2-10 采购单 API
- [x] P2-11 采购链路联调
- [x] P2-12 库存核心模型
- [x] P2-13 入库流程（采购/其他/销售退货入库及审核、反审核）
- [x] P2-14 出库流程（销售/采购退货/其他出库及审核、反审核）
- [x] P2-15 库存盘点
- [x] P2-16 库存查询收口
- [x] P2-17 配送基础模型（承运商、司机、路线、客户路线关系、配送异常）
- [x] P2-18 配送任务
- [x] P2-19 签收回单
- [x] P2-20 第二阶段联调

### P3 售后与财务（已完成）

- [x] P3-01 售后模型
- [x] P3-02 售后流程
- [x] P3-03 售后库存联动
- [x] P3-04 客户账单模型
- [x] P3-05 客户结款
- [x] P3-06 供应商结算
- [x] P3-07 第三阶段联调

### P4 查询与支撑能力（已完成）

- [x] P4-01 溯源模型（检测报告、报告商品、附件、溯源记录、外部报送日志）
- [x] P4-02 溯源流程
- [x] P4-03 销售与售后报表
- [x] P4-04 库存与采购报表
- [x] P4-05 首页驾驶舱
- [x] P4-06 导入导出任务
- [x] P4-07 文件上传
- [x] P4-08 打印能力
- [x] P4-09 系统支撑
- [x] P4-10 全项目收口

## 续接规则

每次开发开始先读取本文件、[自动开发任务清单](./自动开发任务清单.md) 和对应的 `docs/business-flows/*.md`，检查 `git status`，再运行构建与测试。每次只领取清单中第一个未完成任务。只有交付物、测试和文档均通过验收才能勾选；结束时更新断点、模块表、核对日期和测试基线。
