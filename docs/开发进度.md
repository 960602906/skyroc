# SkyRoc 开发进度

> 本文只记录实现状态、开发断点、迁移/部署事项和后续产品范围。命令结果、通过数量、质量报告与测试环境问题统一记录在 [`测试进度.md`](./测试进度.md)。
>
> 最后核对：2026-07-21。

## 当前开发断点

| 范围 | 当前状态 | 下一步 |
| --- | --- | --- |
| 后端 P5 性能与契约治理 | P5-01「下拉选项分级治理」已完成代码与定向验证，因完整套件、真实 PostgreSQL 万级样本和浏览器 Network 验收未全部完成，暂不勾选。 | 补齐剩余验收；部署时先应用 `20260721024046_AddSelectionOptionSearchIndexes`，确认旧路径一个发布周期无调用后另立删除任务。 |
| 前端菜单页面 | 横向任务 X01 已完成代码迁移与静态验证；M06 仍缺真实后端会话下的运行时验收。 | 以 M06 为当前唯一前端任务，完成运行时回归后续接 M07。 |
| 自动业务测试 | 与开发任务分离；T13「全链路、异常与非功能」已勾选；当前测试任务为 T14「全量验收与前端联调交付」。 | 见 [`测试进度.md`](./测试进度.md) 与 [`自动测试任务清单.md`](./自动测试任务清单.md)。 |
| 通用 AI 助手与 MCP | **仅完成实施计划，尚未开始编码。** 已登记为 P6 阶段，包含可插拔 DeepSeek/GLM/OpenAI-Compatible、SkyRoc MCP、会话、语音、查单和人工确认下单。 | 默认在 P5-01 与前端 M07 完成后，严格按[实施计划](./superpowers/plans/2026-07-22-ai-assistant-mcp.md)逐步执行；每步测试通过后才进入下一步。 |

## 验收与测试基线

> 设计完成标准要求本文保留当前断点、测试数量、数据库基线与最近验收日期。命令明细、失败证据与质量报告文件名仍以 [`测试进度.md`](./测试进度.md) 为准；本文只保留可交付摘要，避免与测试进度重复大段命令输出。

| 项目 | 当前基线 |
| --- | --- |
| 最近验收日期 | 2026-07-21 |
| 完整测试数量基线 | 453 通过、0 跳过（约 50 分 3 秒）；仅用户明确要求时才运行完整 `SkyRoc.Tests` |
| 数据库基线 | 专用库 `skyroc`；业务表非空 ≥80；`demoDataAcceptance.isReady=true`；临时残留 / 元数据 / 孤儿外键 / 重复编码 findings 为 0；库存批次非负 |
| 一键验收入口 | `scripts/Invoke-SkyRocFullAcceptance.ps1`（默认不跑完整套件；完整回归仅 `-IncludeFullTestSuite`） |
| 联调交付说明 | [`testing/前端联调数据说明.md`](./testing/前端联调数据说明.md)（稳定键、场景索引、账号来源边界，不含密码/连接串） |
| 防退化测试 | `PostgreSqlInfrastructureDocumentationTests.ProgressBaseline_DocumentsAcceptanceCountsAndDatabaseBaseline` |
| T14 勾选状态 | **未勾选**。一键验收、最终质量门禁、联调手册、进度基线与历史 `SKYROC-AUTOTEST-` 残留清理均已落地；尚需用户明确触发完整 `SkyRoc.Tests` 后才能勾选 T14 |

## 最近开发记录

### 2026-07-21 下拉选项分级与限量搜索治理

- 新增统一 `SelectionOptionDto`、无总数统计的 `SelectionOptionSearchResultDto`，以及 `/options/search`、`/options/resolve`、`/options/bounded`；增长型数据默认 20、最多 50，解析最多 100，有界数据超过 500 返回业务校验失败。
- 通用基础资料在数据库侧 `AsNoTracking` 后直接投影；销售订单单独投影“订单号 + 客户名称”，空关键词按最近创建时间排序。旧全量 GET 与报价/协议旧 `/options` 保留一个发布周期并在 Swagger 标记弃用。
- 迁移 `20260721024046_AddSelectionOptionSearchIndexes` 启用 `pg_trgm`，为商品、客户、供应商、报价、协议和销售订单建立 `lower(...)` GIN trigram 索引；Down 只删除本次索引，不删除共享扩展。
- 前端新增 `RemoteOptionSelect`/`useRemoteOptions`，完成客户、商品、供应商、报价、协议和销售订单选择迁移；公司、仓库、标签、采购员、司机、角色改走有界轻量接口，商品单位按商品联动，写操作会失效增长型资源选项缓存。

### 2026-07-20 打印模板启用态 bool 哨兵修复

- `PrintTemplateConfiguration`：移除 `is_enabled` 的 `HasDefaultValue(true)+HasSentinel(false)`，避免显式写入 `false`（停用）被 EF 当作未设置而落到数据库默认值 `true`。
- 迁移 `20260720142527_FixPrintTemplateIsEnabledBoolSentinel`：去掉 `print_template.is_enabled` 列默认值；部署需先 `dotnet ef database update`。

### 2026-07-20 商品上下架 bool 哨兵修复

- `GoodsConfiguration` / `QuotationGoodsConfiguration`：移除 `is_on_sale` 的 `HasDefaultValue(true)+HasSentinel(false)`，避免显式写入 `false`（下架）被 EF 当作未设置而落到数据库默认值 `true`。
- 迁移 `20260720140536_FixGoodsIsOnSaleBoolSentinel`：去掉 `goods`、`quotation_goods.is_on_sale` 列默认值；部署需先 `dotnet ef database update`。

### 2026-07-20 检测报告更新唯一索引冲突修复

- `TraceabilityService.UpdateInspectionReportAsync`：商品明细与附件改为按入库明细主键/展示顺序就地同步，避免 Clear+Add 在 `(inspection_report_id, stock_in_detail_id)` 唯一索引上同批 INSERT/DELETE 冲突导致更新失败。

### 2026-07-20 库存并发扣减锁修复与测试库切换

- 专用测试库白名单由 `neondb` 调整为 `skyroc`；测试连接优先读取 `SkyRoc/appsettings.Development.json`。
- `StockBatchRepository.GetByIdentityForUpdateAsync`：在执行 `FOR UPDATE` 前分离已跟踪的同标识批次，避免出库单 `ThenInclude(StockBatch)` 的旧快照吞掉行锁重读结果，修复并发超额出库双胜写坏库存。

### 2026-07-20 售后列表与前端续接

- 售后分页已收敛为总数查询加单条轻量投影；列表 DTO 与详情 DTO 分离，按创建时间和主键倒序分页。
- 新增迁移 `20260720033333_OptimizeAfterSaleListQuery`，为售后分页创建 `(create_time DESC, id DESC)` 索引；部署时需先应用迁移，再原子发布前后端。
- 前端售后列表补齐来源订单跳转、草稿新增/编辑页；取货任务已完成查询、详情、全页安排司机、开始与完成操作，新增任务入口统一从售后审核自动生成，避免绕过售后与退货入库链路。
- 前端菜单后续工作已固定为 `client/docs/接口对接与页面完善清单.md` 的顺序，不再通过临时指定页面推进。

### 已完成阶段

| 阶段 | 开发状态 | 已交付范围 |
| --- | --- | --- |
| P1 系统权限与基础资料 | 已完成 | 分层、认证、动态路由、权限、基础资料、定价和第一阶段契约。 |
| P2 订单主链路 | 已完成 | 销售订单、采购计划/采购单、出入库、库存、配送、签收和主链路联调能力。 |
| P3 售后与财务 | 已完成 | 售后/取货、客户账单与结款、供应商待结与结算。 |
| P4 查询与支撑 | 已完成 | 溯源、报表、驾驶舱、导入导出、文件、打印、运营设置、公告和审计。 |

## 模块实现状态

| 模块 | 状态 | 当前实现 | 开发边界 / 后续产品范围 |
| --- | --- | --- | --- |
| 平台与权限 | ✅ | 统一响应、异常处理、Swagger、JWT、动态路由、Redis/内存降级、细粒度资源/操作权限。 | 生产密钥、连接串和 Redis 部署配置由环境注入，不写入仓库。 |
| 基础资料与定价 | ✅ | 商品、单位、分类、客户、公司、标签、子账号、供应商、采购员、仓库、报价、协议价、采购规则。 | 学校/周期报价差异、批量业务操作属后续需求。 |
| 订单、采购与库存 | ✅ | 订单审核、采购计划/采购单、各类出入库、批次、台账、盘点、库存查询。 | 期初库存维护和独立库存成本报表属后续产品范围。 |
| 配送与售后 | ✅ | 承运商、司机、路线、任务、异常、签收回单、售后、取货、退货入库联动。 | 配送单分组打印属后续需求。 |
| 财务 | ✅ | 客户账单、结款、供应商待结和供应商结算的正逆向联动。 | 对账报表与导出属后续需求。 |
| 溯源与报表 | ✅ | 检测报告、订单商品溯源、公开二维码、外部报送状态、销售/售后/库存/采购报表和驾驶舱。 | 外部平台实际网络报送尚未接入。 |
| 导入导出、文件与打印 | ✅ | 商品 CSV 导入导出、受保护文件上传下载、打印模板和业务打印快照。 | 当前导入导出仅支持商品 CSV；文件没有通用列表/删除契约。 |
| 系统支撑 | ✅ | 服务时段、小程序下单、分拣权重、公告、操作/登录审计。 | 后台页面的持续完善按前端菜单清单执行。 |

## 文档职责与更新规则

| 文档 | 只记录 | 何时更新 |
| --- | --- | --- |
| [`开发进度.md`](./开发进度.md) | 实现状态、当前开发断点、迁移/部署事项、产品边界。 | 功能、架构、迁移、任务状态或开发断点变化时。 |
| [`测试进度.md`](./测试进度.md) | 测试任务、命令结果、通过/失败、质量报告、环境与测试基线。 | 执行测试、质量检查、性能采样或发现测试环境阻塞时。 |
| [`client/docs/前端开发进度.md`](../client/docs/前端开发进度.md) | 前端菜单页面的唯一当前任务、接口/页面状态和前端验证。 | 完成或阻塞一个前端菜单任务时。 |

一次交付同时包含实现与测试时，分别写入开发和测试文档；两边只使用相互链接，不复制大段测试过程或业务实现说明。

## 续接规则

1. 后端开发：读取本文、[`自动开发任务清单.md`](./自动开发任务清单.md) 和对应 `docs/business-flows/`；仅领取第一个未勾选任务。
2. 前端页面：读取 `client/AGENT.md` 与 `client/docs/` 的两份续接文档；只领取页面清单的第一项未完成任务。
3. 自动业务测试：读取 [`测试进度.md`](./测试进度.md) 与 [`自动测试任务清单.md`](./自动测试任务清单.md)；只领取第一个未完成测试任务的一个业务切片。
4. 交付前更新实际发生变化的进度文档；验证事实只写入测试进度，不再回填到本文的开发记录中。
